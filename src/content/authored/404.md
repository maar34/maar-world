---
outputPath: "404"
title: "Page not found - MAAR WORLD"
description: "That address does not exist on maar.world."
area: "maar"
kind: "page"
lang: "en"
origin: "authored"
noindex: true
tags: []
---

<!-- mark.strike, and the only struck word on the site. It is here because the
     sentence already says the page is not in the deck: remove the bar and
     nothing is lost, which is the test that keeps a cross-out from being the
     thing that carries the meaning. `src/content/authored/**` is the half of
     the content tree no script writes, so a span put here is stable — the same
     span in `src/content/migrated/**` would be gone on the next migration run.
     Removing the mark is deleting the two tags. -->
# This <span class="mark mark--strike">page</span> is not in the deck

The address you followed does not exist here. It may have moved when
`collect.maar.world` and `tree.maar.world` merged into this one site, or it may
never have existed.

If you arrived by scanning a **physical card** and landed here, the code on the
card did not resolve. That should not happen — every printed code has a page.
Please [get in touch](/bookings) and say which card it was.

Otherwise, try [the home page](/), [the lab](/lab), or [collect](/collect).
