---
outputPath: "404"
title: "Page not found - MAAR WORLD"
description: "The address you followed does not exist on maar.world."
area: "maar"
kind: "page"
lang: "en"
origin: "authored"
noindex: true
tags: []
family: "notfound"
# The heading's two marks are named here as words and drawn by
# families/NotFound.astro. `mark` is struck — the only struck word on the site,
# kept because the sentence already says the page is missing; remove the bar
# and nothing is lost. `echo` is printed twice.
notfound:
  meta: "404 · page not found"
  heading: "This page is not in the deck"
  mark: "page"
  echo: "deck"
---

It may have moved when `collect.maar.world` and `tree.maar.world` merged into
this one site, or it may never have existed.

If you arrived by scanning a **physical card** and landed here, the code on the
card did not resolve. That should not happen — every printed code has a page.
Please [get in touch](/bookings) and say which card it was.

Otherwise, try [the lab](/lab), [collect](/collect) or [the tree](/tree).
