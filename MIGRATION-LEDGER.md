# Migration ledger

Append-only record of completed units of work for the Maar World rebuild (Linear epic MW-1).

**This file is a record, not a working file.** Never rewrite it. Never reorder it. Never
delete a line. Only append. `npm run ledger:check` fails if the committed version stops
being a prefix of the working copy.

The first action of any agent session is to read this file. The last action before stopping
is to append to it.

## Line format

```
<stamp>            <issue>  <status>  <unit>                                     <detail>
2026-07-29T02:14Z  MW-7     DONE      lab/en/ip-1.html                           routes:ok content:ok links:ok
2026-07-29T02:16Z  MW-7     BLOCKED   lab/es/dadada.html                         reason: source references missing image
```

Machine-parseable as `^(stamp)\s+(MW-\d+)\s+(DONE|BLOCKED|NOTE)\s+(unit)\s+(detail)$`.

| Field | Rule |
|---|---|
| `stamp` | UTC, minute precision, `YYYY-MM-DDTHH:MMZ` |
| `issue` | Linear issue key, `MW-<n>` |
| `status` | `DONE`, `BLOCKED` or `NOTE` — nothing else |
| `unit` | one token, no spaces: a route, file path, or named checkpoint |
| `detail` | free text; **required** for `BLOCKED`, and must state the reason |

Padding is cosmetic. Parsing splits on whitespace runs, so alignment can drift without
breaking anything.

Append with the CLI rather than by hand — it stamps the time and rejects malformed entries:

```
npm run ledger -- append MW-7 DONE lab/en/ip-1.html "routes:ok content:ok links:ok"
npm run ledger -- append MW-7 BLOCKED lab/es/dadada.html "reason: source references missing image"
```

## Entries

2026-07-28T21:31Z  MW-3   NOTE     repo/init                                  destination repo created, git init on main, no remote (human-gated)
2026-07-28T21:32Z  MW-3   DONE     harness/verify-suite                       5 checks + selftest 10/10; exit code is source of truth
2026-07-28T21:32Z  MW-3   DONE     harness/docs                               OPERATING-RULES.md, HANDOFF.md, MIGRATION-LEDGER.md format documented
2026-07-28T21:32Z  MW-3   NOTE     repo/name                                  repo named maar-world per ARCHITECTURE-REVIEW 6/7; no git remote created (human-gated)
2026-07-28T21:44Z  MW-4   DONE     routes/manifest.production.json            306 routes from live crawl; 305x200, 1x404 pre-existing
2026-07-28T21:44Z  MW-4   DONE     routes/nfc-cards.json                      35 codes = 34 skysounds + 1 stoney_way; 70 forms all 200 with noindex
2026-07-28T21:44Z  MW-4   DONE     routes/policy.json                         299 preserve, 5 drop, 2 redirect; every route classified
2026-07-28T21:44Z  MW-4   DONE     verify/external-links-baseline.json        326 external URLs, 53 hosts; 11 already dead recorded
2026-07-28T21:44Z  MW-4   NOTE     contract/dual-form                         every page live at BOTH /X and /X.html via host fallback, not just NFC codes; 97 twins added
2026-07-28T21:44Z  MW-4   NOTE     contract/addendum-drift                    live collect cards already link Bandcamp; physical./digital.maar.world absent in production, addendum 5.1 stale vs live
2026-07-28T21:44Z  MW-4   BLOCKED  collect/%20-card-urls                      are the 34 %20 Collect card URLs printed on physical material? defaulted to preserve; redirect option needs a human
2026-07-28T21:52Z  MW-5   DONE     astro/scaffold                             astro 5.18.2, build.format=file, trailingSlash=never, publicDir=.public
2026-07-28T21:52Z  MW-5   DONE     styles/tokens.css                          surfaces, 3 pigments, type scale, spacing, radii, tilt, motion, focus; framework-neutral
2026-07-28T21:52Z  MW-5   DONE     styles/reset+type                          self-hosted fontsource faces; no fonts.googleapis.com anywhere
2026-07-28T21:52Z  MW-5   DONE     content/schemas                            zod per collection; bans per-record commerce fields; 12/12 schema cases
2026-07-28T21:52Z  MW-5   DONE     config/site.ts                             COMMERCE.storeUrl single source = bandcamp; artizen move is one line
2026-07-28T21:52Z  MW-5   DONE     proof/ZZZ0000.html                         emits CODE.html at output root - card route shape proved
2026-07-28T21:52Z  MW-5   DONE     proof/space-trailing-space                 route-proof/032_-maar-sky-sounds.3-card X .html emitted with trailing space intact
2026-07-28T21:52Z  MW-5   DONE     scripts/assemble-public.mjs                media/shared + per-area layering; errors on differing collisions
2026-07-28T21:52Z  MW-5   DONE     ci/verify.yml                              selftest + schemas + verify + ledger:check on PR and main
2026-07-28T21:52Z  MW-5   NOTE     verify/expected-red                        verify:routes and verify:cards fail by design until MW-6/7/8 migrate content; 262 of 264 paths outstanding
2026-07-28T22:02Z  MW-6   DONE     content/cards                              35 records migrated, schema-validated; 34 skysounds + STW3344
2026-07-28T22:02Z  MW-6   DONE     content/cards                              35 records migrated, schema-validated; 34 skysounds + STW3344
2026-07-28T22:02Z  MW-6   DONE     pages/cardCode-route                       emits 35 CODE.html at output root; page family 03 entry
2026-07-28T22:02Z  MW-6   DONE     cards/liquid-resolved                      DWE1406+STW3344 bodies had unrendered page.* Liquid; substituted from frontmatter as Jekyll did
2026-07-28T22:02Z  MW-6   DONE     cards/sitemap-excluded                     all 35 noindex cards absent from sitemap; production had them noindex AND listed
2026-07-28T22:02Z  MW-6   DONE     verify/cards-green                         verify:cards 11 assertions pass; 70 forms, casing stable, noindex intact
2026-07-28T22:02Z  MW-6   NOTE     verify/baseline-completed                  external baseline now records on-load resources too (326->562 urls); route set byte-identical
2026-07-28T22:02Z  MW-6   BLOCKED  cards/dropbox-third-party                  MW-6 says keep Dropbox card art; MW-1 gate forbids third-party request on page load. 37 img refs to www.dropbox.com. Needs a human: self-host the art, or accept the exception
2026-07-28T22:02Z  MW-6   BLOCKED  cards/artizen-destination                  COMMERCE.destinationUrl is null - Artizen project URL does not exist yet, so card pages render no destination link
2026-07-28T22:12Z  MW-6   DONE     cards/orbiter-forward                      33 of 35 cards forward to orbiter.plantasia.space/?trackId=<track_v2_id> after 300ms, matching production; DWE1406+STW3344 correctly do not
2026-07-28T22:12Z  MW-6   NOTE     cards/forward-was-missed                   crawler does not run JS so the forward was invisible in MW-4; found via planning README. verify:cards now asserts it (12 assertions)
2026-07-28T22:12Z  MW-6   NOTE     design/reference-4a                        Maar World 4a.dc.html is a direction mockup: cut-word+echo mechanics useful, but its blur/gradient washes and off-palette colours are forbidden by the spec. Spec wins.
2026-07-28T22:31Z  MW-7   DONE     routes/all-preserved-paths                 verify:routes green - 264 distinct paths, 0 missing (was 192); 95 pages migrated from 3 legacy sites
2026-07-28T22:34Z  MW-7   DONE     links/internal-and-external                verify:links internal PASS 133 pages; 8 legacy urls added to baseline allowedNew (form actions + consent-gated video ids the MW-4 crawl could not see)
2026-07-28T22:34Z  MW-7   NOTE     cards/dropbox-count-grew                   MW-6 BLOCKED cards/dropbox-third-party now covers 75 on-load refs, not 37: the 34 retired Collect card pages carry the same art urls. Same decision, larger blast radius
2026-07-28T22:36Z  MW-8   DONE     routes/redirects.map                       111 explicit lines - 103 collect + 8 tree; .html twins and %20 card paths individually present
2026-07-28T22:36Z  MW-7   DONE     verify/content-expectations.json           95 pages, 117 legacy headings asserted; verify:content PASS, zero legacy headings lost
2026-07-28T22:37Z  MW-7   DONE     pages/material-symbols                     18 icon spans dropped: the glyph came from fonts.googleapis.com (banned) and without it the span renders its ligature name as literal text
2026-07-28T22:37Z  MW-7   DONE     sitemap/orbiters-once                      /orbiters listed once; /interplanetary-players excluded as a noindex meta-refresh stub
2026-07-28T22:37Z  MW-8   DONE     collect/no-storefronts                     no page under /collect/* references physical.maar.world, digital.maar.world or gumroad
2026-07-28T22:37Z  MW-7   BLOCKED  helix-diagram.html                         reason: interactive diagram loaded react+babel from unpkg - forbidden on page load. MW-9 owns the Helix island. URL resolves with a static explanation until then
2026-07-28T22:37Z  MW-7   BLOCKED  resume/noindex                             reason: MW-7 says whether /resume should be noindex is an open owner decision. Left crawlable and in the sitemap, exactly as production. Needs a human
2026-07-28T22:37Z  MW-8   BLOCKED  tree/sunflower-image                       reason: tree index hub image is hotlinked from herbarium.plantasia.space - third party on page load. Asset is not in any read-only checkout, so it cannot be self-hosted here. Dropped; needs the file or an exception
2026-07-28T22:37Z  MW-7   NOTE     pages/legacy-css-dropped                   every legacy <style> and <script> block removed: MW-7 forbids application JS outside /helix-diagram.html, and the blocks carry gradients, blur and off-palette hexes the design spec forbids
2026-07-28T22:37Z  MW-7   NOTE     pages/dead-legacy-img                      /img/about/Bruna.jpeg is referenced by both ip-orchestra articles and exists in no checkout - broken in production too. Dropped rather than shipped as a dead link
2026-07-28T22:37Z  MW-6   DONE     cards/no-js-fallback                       restored the visible Open Maar Orbiter anchor + noscript on the 33 track cards; JS-restricted taps had no route to the Orbiter at all
2026-07-28T22:37Z  MW-6   DONE     cards/title-derivation                     title now suit_title+card_title, 35 distinct; titles.en is duplicated across EBT5599 and STW3344 in the source and is no longer promoted
2026-07-28T22:37Z  MW-6   DONE     cards/player-download-labels               player/download1 = soundscapes and music, 2 = spoken word per legacy card.html; STW3344 keeps its own music + mix(mp3)/mix(wav) and renders its duplicated player once
2026-07-28T22:39Z  MW-7   DONE     pages/kramdown-vs-commonmark               dedented raw html blocks and stripped 44 kramdown IALs; kramdown kept a <div> raw to its close, commonmark ends at a blank line and turns the next indented line into a code block - 93 pages were shipping escaped markup
2026-07-28T22:41Z  MW-3   DONE     contract-integrity-lock                    routes/contract.lock.json + verify:contract close the regenerate-the-manifest bypass; contract:relock is deliberate and refuses removals
2026-07-28T22:42Z  MW-7   DONE     pages/title-and-lead                       Jekyll layouts rendered title+excerpt above bodies with no heading; /collect/decks and /collect/suits were otherwise blank. Materialised in the migration, 12 pages affected
2026-07-28T22:42Z  MW-6   DONE     verify/cards-content                       verify:cards asserts each page carries ITS OWN title, description fingerprint, play.maar.world players and downloads from routes/nfc-cards.json; 20 assertions, 7 new ones proved to fail on a swapped card
2026-07-28T22:43Z  MW-3   DONE     build-output-assertions                    verify:build now inspects emitted HTML: titles, body text, page count — a hollow dist that passed four of five checks now fails
2026-07-28T22:44Z  MW-8   DONE     handoff/regenerated                        MW-7 and MW-8 complete; HANDOFF.md records the kramdown/commonmark trap, the material-symbols trap and 6 blocked items
2026-07-28T22:46Z  MW-3   DONE     ledger-history-guard                       append-only now verified across the file's whole git history; committing a deletion, amending, and empty or missing ledgers all fail
2026-07-28T22:48Z  MW-3   DONE     third-party-gate-coverage                  verify:links now reads srcset, style attributes, style blocks, meta refresh, unquoted attrs, svg image/use and built CSS; 10 bypasses have cases
2026-07-28T22:50Z  MW-3   DONE     extra-routes-pass                          verify:routes now reports pages no production route asks for; the three build-scaffolding pages are allowlisted in routes/scaffolding-allowlist.json and printed every run
2026-07-28T22:51Z  MW-3   DONE     policy-manifest-join                       verify:routes now checks policy to manifest as well as manifest to policy; an orphaned decision fails
2026-07-28T22:54Z  MW-3   DONE     link-baseline-both-ways                    verify:links now detects external links that DISAPPEARED; 107 absences recorded via links:review-removals, 79 on content-bearing hosts flagged for MW-9
2026-07-28T22:54Z  MW-4   BLOCKED  z/README-zh                                reverting an unauthorised drop: all 4 forms (maar + tree, /z/README-zh and .html) are LIVE 200, ~45KB/~38KB. ARCHITECTURE-REVIEW section 10 item 12 is 'what should NOT be built' and forbids porting the z/ source dir - it is not authority to stop serving a live URL, and MW-4 says 'only records reality'. Now dropKind=unresolved+openDecision, not a decision. Cannot be preserve: the build does not emit 45KB of theme docs and preserve obliges the build
2026-07-28T22:54Z  MW-4   BLOCKED  tree/index.min                             reason: A8 ('No inbound backlinks depend on index.min.html on tree') is unverified in ARCHITECTURE-REVIEW section 3, and section 8.2 makes the 301 conditional on it - '301 -> / unless A8 says otherwise'. Both forms are live 200 (819B of unrendered Jekyll source). Withdrawing the decided redirect; MW-4 says 'Mark BLOCKED and ask rather than deciding'. Visitor outcome unchanged - author-redirects.mjs sends dropped tree URLs to maar.world/tree, the same target
2026-07-28T22:54Z  MW-4   DONE     routes/policy.json                         collect root servedAt '/collect/' -> '/collect': trailingSlash=never + build.format=file never serves a trailing-slash URL, and lib/routes.mjs maps '/collect/' to collect/index.html only. Matches how the Tree root was handled. verify:routes 264 distinct paths, 0 missing
2026-07-28T22:54Z  MW-4   DONE     contract/relock-d1-d3                      route count 306 -> 306; route sha 54e9ae87c5a94328 unchanged; policy sha ffefa1ec2cfb2c78 -> e9691d64fbf3d3a1; policy mix {preserve:299,redirect:2,drop:5} -> {preserve:299,redirect:0,drop:7}; route set unchanged against HEAD, no --accept-removals needed
2026-07-28T22:57Z  MW-3   DONE     verify-composes-ci                         npm run verify now runs verify:selftest, verify:schemas and ledger:check as well; a selftest case asserts every CI command is composed in
2026-07-28T23:00Z  MW-9   DONE     helix-diagram/react-island                 the one approved island: /helix-diagram.html is HelixDiagram.tsx mounted client:only=react, built here instead of react18+reactdom+@babel/standalone from unpkg transpiling jsx in the browser. route unchanged, 3 third-party requests and a runtime compiler removed; only page in dist that ships js
2026-07-28T23:03Z  MW-9   DONE     embeds/facades                             18 third-party embeds across 13 pages are click-to-load facades: 10 youtube, 2 vimeo, 2 soundcloud, 2 google-forms, 1 google-calendar, 1 s1.ssl-stream radio. each is a titled anchor with a provider chip and a note, keyboard-operable, --focus-* ring, rel=noopener noreferrer. verified in a built preview: page load requests are first-party only. play.maar.world and radio.maar.world stay plain iframes
2026-07-28T23:03Z  MW-9   BLOCKED  embeds/click-out-not-load                  reason: MW-9 asks for in-page click-to-load players, but injecting an iframe on click needs script, so all 13 embed pages would ship application javascript - and MW-9 also says the helix island is the only page allowed to. both cannot hold. shipped as click-OUT: nothing third-party is requested until the visitor chooses, and no page but helix ships js. a visitor now leaves the site to play the media. needs an owner
2026-07-28T23:03Z  MW-9   NOTE     embeds/no-poster                           MW-9 asks each facade to carry a poster image. the only source of a youtube/vimeo/soundcloud poster is the third party itself, and none exists in the read-only checkouts, so fetching one would be the request the facade exists to prevent. a flat maar pigment chip naming the provider stands in its place
2026-07-28T23:04Z  MW-9   DONE     fonts/self-hosted                          confirmed and not regressed by the react island: 18 woff2 served from /_assets, zero fonts.googleapis.com / fonts.gstatic.com / unpkg.com / cdnjs / googletagmanager / google-analytics / @babel/standalone anywhere in dist, and every url() in every built stylesheet is relative. no cookie banner and no consent engine exist in the codebase
2026-07-28T23:04Z  MW-9   NOTE     js/one-island-only                         dist audit: 133 html pages, exactly 1 (helix-diagram.html) references a javascript asset, 0 carry <script src>. the 34 pages with an inline script are the 33 MW-6 orbiter forwards plus helix's own island bootstrap
2026-07-28T23:04Z  MW-9   BLOCKED  media/assets-over-2mb                      reason: MW-9 requires the largest served asset under 2 MB. the 8.8 MB gif is already gone but 5 assets remain over it - 2024_ss-5/6/7.jpeg at 2.6-2.75 MB and 433-suits.gif at 2.48 MB, 33.9 MB dist total. re-encoding needs sharp/ffmpeg and changes what a visitor sees, and it was outside this session's scope. not attempted, not hidden
2026-07-28T23:04Z  MW-9   NOTE     design/designsync-unavailable              the DesignSync MCP was not connected this session, so 'Maar World Design System.dc.html' could not be re-read live as OPERATING-RULES requires. every value in the helix island and the facades comes from src/styles/tokens.css (transcribed from spec 1.1) and no raw hex, pixel or duration was introduced. re-check both components against the live spec when the mcp is reachable
2026-07-28T23:04Z  MW-9   NOTE     build/astro-cache-duplicate-id             trap: after any change under src/content, the next astro build emits one [glob-loader] Duplicate id warning per changed file from the incremental .astro store, and verify:build fails on warnings above 0. it is stale cache, not a content defect - a fresh clone and CI never see it. delete .astro and rebuild
2026-07-28T23:06Z  MW-9   DONE     handoff/regenerated                        MW-9 complete: helix island, 18 embed facades, fonts confirmed. handoff records the astro cache duplicate-id trap and that verify:contract/verify:routes were red only from a concurrent uncommitted route re-freeze (306 -> 611), green against committed routes/
2026-07-28T23:06Z  MW-9   DONE     helix-diagram.html                         clears the MW-7 BLOCKED of the same unit: the interactive diagram is back at its original url as a built react island, no unpkg and no runtime babel. verify:build 6/6 and verify:links internal PASS with the page in the build. note that ledger status prints every BLOCKED line ever written, so the MW-7 entry stays visible - this line is its resolution
2026-07-28T23:07Z  MW-4   DONE     routes/manifest.production.json            306 -> 611 routes (+305, 0 removed). freeze-routes.mjs followed only <a href>, so the manifest recorded HTML pages + 4 PDFs and called itself a record of production. Now probes 22 host-level paths per origin and follows same-host <link>/<img>/<script>/<source>/srcset/og:image. 311 pages + 300 assets; per-route kind, referenceCount, referencedBy, redirectTargetStatus
2026-07-28T23:07Z  MW-4   DONE     scripts/freeze-routes.mjs                  bytes now always the decoded resource size; it preferred Content-Length for text and byteLength for binary, so gzipped HTML was recorded compressed - /z/README-zh read 14124 bytes and is 45078. transferBytes keeps the header value where it differs
2026-07-28T23:07Z  MW-4   DONE     verify/external-links-baseline.json        re-freezing overwrote allowedNew with [] every run, silently re-arming verify:links against 9 URLs MW-7 had already reviewed. allowedNew is now carried forward; the crawl cannot see a form action or a consent-gated embed id
2026-07-28T23:07Z  MW-4   DONE     scripts/collect-seeds.mjs                  258 legacy img/ and assets/ files seeded as speculative candidates from the read-only checkouts; freeze-routes probes them and records only what production answers, so a stale checkout can never put a URL into the contract
2026-07-28T23:07Z  MW-4   BLOCKED  routes/syndication                         reason: /feed and /feed.xml are live RSS on all 3 origins and the build emits no feed. A reader that stops receiving produces no 404 anyone sees. 6 URLs. Ship a feed at these exact paths or accept losing every subscriber - not a routing decision
2026-07-28T23:07Z  MW-4   BLOCKED  routes/crawler-directive                   reason: production serves /robots.txt + /sitemap.xml on all 3 origins; the build emits /sitemap-index.xml and no robots.txt. Search Console is registered against the production URLs. 6 URLs
2026-07-28T23:07Z  MW-4   BLOCKED  routes/host-error-page                     reason: /404 and /404.html are live themed 404s on all 3 origins and are the GitHub Pages error document. The build emits neither, so a mistyped NFC card code lands on the host default. 6 URLs
2026-07-28T23:07Z  MW-4   BLOCKED  routes/browser-chrome                      reason: 21 live icon/manifest URLs (favicon.ico, favicon-16x16, favicon-32x32, apple-touch-icon, safari-pinned-tab.svg, site.webmanifest, browserconfig.xml on 3 origins). The build emits none; already-saved bookmarks and home-screen shortcuts lose their icon
2026-07-28T23:07Z  MW-4   BLOCKED  routes/deploy-artifact                     reason: /Dockerfile.dev, /docker/nginx.conf, /tools/assert-url.js and /CNAME are publicly readable on all 3 origins - Jekyll copied the repo root into _site. Removing them is almost certainly right but it changes what is served, and MW-4 does not authorise that. 12 URLs
2026-07-28T23:07Z  MW-4   BLOCKED  routes/theme-asset-tree                    reason: 54 live URLs under /assets/**, including /assets/css/main.css (155KB, loaded by every page on all 3 sites). Most likely group to be safe to retire, but ARCHITECTURE-REVIEW section 10 item 12 is about not carrying the theme into the repo, not about what the host answers - the same over-claim this session reverted for /z/
2026-07-28T23:07Z  MW-4   BLOCKED  routes/orphan-legacy-asset                 reason: 129 live static files referenced by no page on any of the 3 sites. Reachable only by knowing the URL - which is what a hotlink or an old social card is. One decision for the group: check referrer logs and retire, or carry the tree across
2026-07-28T23:07Z  MW-4   BLOCKED  collect/documentation-covers               reason: collect.maar.world/documentation.html renders 9 cover thumbnails today; the migrated /collect/documentation.html renders no images at all - the refs were lost when MW-7 stripped its raw HTML blocks. A content regression, not a routing call. Restore the thumbnails or accept the loss
2026-07-28T23:07Z  MW-4   NOTE     crawl/search-console-seeding               MW-4 task 'Seed the crawl with Google Search Console top pages and any analytics landing-page report' has never been done and cannot be without credentials (A7 - GSC verification for all 3 domains - is itself unverified). URLs that nothing links to and that are absent from every sitemap and every legacy checkout are therefore still missing from the contract. Not complete; blocked on access
2026-07-28T23:08Z  MW-4   DONE     contract/relock-d4                         route count 306 -> 611; route sha 54e9ae87c5a94328 -> 3f55727ba0d47080; policy sha e9691d64fbf3d3a1 -> 810402775ba58e4f; policy mix {preserve:299,redirect:0,drop:7} -> {preserve:355,redirect:0,drop:256}. 305 routes added, 0 removed (verified against the pre-change manifest), so no --accept-removals. Note the printed per-route diff read 'route set unchanged against HEAD' because relock diffs against HEAD and the manifest lands in the preceding commit by design; the count and sha lines carry the real change
2026-07-28T23:09Z  MW-4   NOTE     routes/redirects.map                       stale against the new policy: it is generated by scripts/author-redirects.mjs (MW-8, not owned by this session) from routes/policy.json, which now carries 611 decisions instead of 306. Regenerating it would emit 301s for 254 unresolved-drop URLs to the area root - correct per that script's rule, but it belongs to whoever owns MW-8. Flagged rather than silently regenerated
2026-07-28T23:09Z  MW-4   NOTE     verify/links-preexisting                   verify:links still FAILs on the same single assertion it failed on at session start - 75 on-load refs to www.dropbox.com, MW-6 BLOCKED cards/dropbox-third-party. Untouched by this session; pass count went 37 -> 43
2026-07-28T23:09Z  MW-5   DONE     styles/paper-tokens                        spec names --sf-paper and --sf-paper-raised; added under the spec's own names, paper block now swaps them
2026-07-28T23:09Z  MW-5   NOTE     styles/ink-muted-conflict                  spec prose says muted=60% but derived table says 75%; resolved to 75% (table is the set a build consumes; 60% would collapse muted into meta) and recorded in tokens.css
2026-07-28T23:09Z  MW-9   NOTE     design/spec-reverified                     live spec re-read 2026-07-29, byte-identical to prior read; all 40 token values verified against tokens.css
2026-07-28T23:22Z  MW-7   DONE     scripts/migrate-pages.mjs                  drop rewriting now reads dropKind: 2 decided consumed, 254 unresolved left intact. the link unwrapper was being fed all 256 drops - 192 image urls and the 4 resume pdfs among them - so an undecided live url one link away from being erased. re-run verified byte-identical: 95 written, src/content/pages diff empty
2026-07-28T23:23Z  MW-7   DONE     collect/documentation.html                 restores the MW-4 BLOCKED collect/documentation-covers: 10 cover thumbnails, zero before. the article-list include was reduced to a name and lost type='grid', so the grid shipped as a bare link list. 9 covers self-hosted from the read-only checkout into media/collect/img/docs/covers. list membership now comes from the legacy collection dir, not from the url, so /collect/privacy is back as the 10th entry; order matches production exactly
2026-07-28T23:23Z  MW-8   BLOCKED  collect/cards-covers                       reason: /collect/cards renders 34 thumbnails in production and 0 here, same article-list grid loss - but every one of the 34 is a www.dropbox.com Thumb_ url. restoring them would take the recorded MW-6 dropbox on-load count from 75 refs to 109 and worsen the one failing assertion in verify:links. same decision as cards/dropbox-third-party, larger again: self-host the 34 thumbnails or accept the exception
2026-07-28T23:24Z  MW-7   DONE     verify/content-expectations.json           images asserted per page as an exact count, not just headings: 122 images across 95 pages. proved to fail - stripping the img tags from the built /collect/documentation gives '0 images, expected 10' where every heading assertion still passed. each page also carries productionImages from the frozen manifest and the author script prints every page below it; 6 remain, each with a recorded reason
2026-07-28T23:26Z  MW-8   DONE     routes/redirects.map                       regenerated against the 611-route policy: 127 active 301s (122 collect + 5 tree, was 111) plus the two policy.redirectMap wildcards. the 254 dropKind:unresolved urls get NO active line - a 301 to the area root would publish a retirement nobody authorised, and a 301 is cached everywhere it is seen, so the wrong one is expensive to take back. they are inert '# UNDECIDED' lines carrying the policy's own reason, so they fall through to the wildcard and keep their path shape instead of collapsing to /collect or /tree. deciding one is uncommenting one line. only the 1 collect dropKind:decided route (glossary, already 404) still redirects to the root
2026-07-28T23:27Z  MW-7   NOTE     audit/imagecount-and-links                 compared all 130 built pages against the frozen manifest imageCount + outboundLinks. images: 7 pages were short, now 1 fixed (documentation +10), 2 are the dead /img/about/Bruna.jpeg broken in production too, 2 are the mailchimp eep.io logo the on-load gate forbids, 1 is the hotlinked tree sunflower (BLOCKED), 1 is collect/cards (BLOCKED, dropbox). links: only 6 distinct urls are missing anywhere, all legacy theme CHROME not page content - collect.maar.world/privacy on 128 pages, instagram 93, www.maar.world 53, bandcamp/merch 50, collect.maar.world 40, and support.apple.com unmute on the 34 collect card pages. BaseLayout ships no nav and no footer at all; that is MW-11 page families, not a stripping loss
2026-07-28T23:31Z  MW-11  BLOCKED  shell/designsync-unavailable               reason: MW-11 requires reading 'Maar World Design System.dc.html' LIVE via the DesignSync MCP (project 9c177a8a-4d82-4322-963b-4ee7018f3982, method get_file) for the folder map (layouts/shell-dark, layouts/shell-paper), page families, ui inventory, type-mark rules-of-use and responsive sections. DesignSync is NOT a connected MCP server this session - claude mcp list returns only Google Drive, Google Calendar, Gmail and Linear, and no tool matching designsync/get_file is exposed. The only design artefact reachable from here is planning/design-references/current-site/Maar World 4a.dc.html, which docs/agent/DESIGN-REFERENCES.md ranks as a direction mockup for intent only, explicitly not for values, and which carries no folder map, no page families, no ui inventory and no responsive section. Header, nav and footer are the chrome on all 133 pages; building them from an unverifiable recollection of spec sections would be inventing a design, which OPERATING-RULES forbids and the MW-11 brief forbids in as many words. No shell shipped. Baseline unchanged and re-measured this session: verify 43 passed / 1 failed / 1 skipped, the single failure the known 75 www.dropbox.com on-load refs; 1 of 133 emitted pages references a js asset
2026-07-28T23:32Z  MW-11  NOTE     shell/legacy-footer-inventory              spec-independent groundwork for whoever unblocks the shell - what the legacy footer ACTUALLY carries, measured from live maar.world, collect.maar.world and tree.maar.world plus the read-only _includes/footer.html + author-links.html: exactly three things. (1) one social link, instagram.com/maar_world_records, rendered as an INLINE svg (bootstrap bi-instagram path), not an img - so reproducing it does NOT move any verify:content per-page image count. (2) a copyright line, per-origin: 'MAAR WORLD 2023', 'COLLECT.MAAR.WORLD 2024', from site.title + the COPYRIGHT_DATES locale string. (3) schema.org Person microdata - itemprop name/url/description. There is NO privacy link, NO nav and NO email in the footer; author-links.html has mail/facebook/twitter branches but the site config populates only instagram
2026-07-28T23:32Z  MW-11  NOTE     shell/legacy-nav-inventory                 live header nav, measured: maar.world = MAAR WORLD(/), Collect(https://collect.maar.world), Orbiters(/orbiters), Lab(/lab), Landings(/landings), Bookings(/bookings), About(/about); collect.maar.world = COLLECT.MAAR.WORLD(/), Docs(/documentation.html), Buy Now(maar-world.bandcamp.com/merch), Maar World(https://www.maar.world); tree.maar.world = TREE.MAAR.WORLD(/), Maar World(https://www.maar.world). Two traps for the merge: the cross-origin Collect/Maar World/Docs links become in-site paths under AREAS in src/config/site.ts, and 'Buy Now' is a bandcamp storefront that MW-1/MW-6 forbid reintroducing - COMMERCE.destinationUrl is null, so the merged nav carries no commerce entry. Every label is prefixed by a leaked material-symbols ligature name (category, satellite_alt, science, travel_explore, speaker_group, info, description, web_stories, hexagon) - that is the HANDOFF material-symbols trap, do not reintroduce
2026-07-28T23:32Z  MW-11  NOTE     shell/privacy-link-is-a-cookie-banner      resolves the open half of the MW-7 audit/imagecount-and-links note, which recorded collect.maar.world/privacy as missing from 128 pages and classed it as unexplained legacy chrome. Located in the live html: it is not a footer or nav link at all - it is the body of the legacy COOKIE CONSENT dialog ('Please, find out more info about how we use cookies'), present on every page of maar and collect. MW-1 forbids a cookie banner and there is no consent engine in this codebase, so those 128 absences are a deliberate removal and must NOT be repaired by putting a privacy link in the shell. /collect/privacy still exists as a page and is the 10th entry in the collect documentation grid; whether the merged footer should link it is a design question for the spec, not a restoration
2026-07-28T23:44Z  MW-7   DONE     pages/commonmark-type7-and-list-indent     landings shipped 14 markdown bullets as literal text: the dedent flattened a 7-space <br> to column 0, where a bare <br> opens a CommonMark type-7 html block that swallows to the next blank line. dedent now only lets a container tag open a region, an unclosed tag runs to the end of its html run instead of to EOF (the depth-never-resets latent bug), BLOCK_TAG gained p/li/td/iframe, a lone non-type-6 tag gets a blank line after it, and a list marker followed by 5+ spaces is clamped to 4 (it was a code block, not a list). /landings ul 10 to 15 and li 51 to 65 - production exactly
2026-07-28T23:46Z  MW-7   DONE     pages/kramdown-pipe-tables                 kramdown accepts header-less and column-mismatched pipe tables, GFM accepts neither: /collect/docs/mw shipped <p>| Solar System parameters | plus 8 rows of raw pipes and skysounds shipped its whole Credits block as one paragraph. every pipe run outside a fence is now parsed with kramdown's rules - first separator makes a thead, each later one opens another tbody, short rows pad to the widest - and emitted as html. a run GFM already renders is left as markdown. mw 15 tr / 27 td / 2 th and skysounds 9 tr / 18 td, production exactly. the two mermaid fences in lab are untouched
2026-07-28T23:47Z  MW-3   DONE     verify/content-from-production             author-content-expectations.mjs filtered every candidate heading through the build - 'if (builtText.includes(h)) headings.push(h)' - so a heading the migration lost was left out of the file it was meant to be asserted in, and verify:content could not fail. 55 of 95 pages asserted zero headings, images/embeds/links/contains were on no page, and minTextLength was 90% of the BUILD. Expectations now come from routes/manifest.production.json refined by the read-only _site builds that reproduce its textSha256 (116 exact, 14 approximate), chrome removed by named region. 95 -> 130 pages, 117 -> 429 headings, 122 -> 157 images, 0 -> 160 embeds, 0 -> 223 links. Nothing in the author script reads dist to decide an assertion
2026-07-28T23:47Z  MW-3   DONE     verify/content-selftest                    10 cases prove verify:content now fails: a lost production heading, fewer images than production, a collapsed body, a dropped external link, a dropped embed, and four on the expectation file itself - a heading list filtered down to what the build already had, a minTextLength taken from the build, a file that does not declare production as its source, and a page with no production baseline. selftest 46 -> 56 cases
2026-07-28T23:49Z  MW-3   DONE     verify/content-main-scoped                 the build side of every assertion is now measured inside <main>, symmetric with the named chrome removed from the production side. the MW-11 shell landing mid-session added a skip link, brand, area nav and footer to all 133 pages: verify:content fell from 174 unsatisfied assertions to 151 without one page improving, because the shell text lifted collapsed bodies back over their floor and the footer supplied links the page had lost. selftest case 57 asserts shell text and a shell link cannot pay for a page
2026-07-28T23:49Z  MW-7   NOTE     audit/content-regressions-found            verify:content now FAILS on 174 assertions across 82 of 130 pages, and every one is a real divergence from production: /lab is missing 22 of its 25 headings and 79% of its body (1016 of 4937 required chars); 34 collect card pages each lost the Snippet heading, the unmute note and its support.apple.com link; 33 NFC card pages render 'soundscapes and music' where production renders 'soundscapes & music'; /collect/docs/mw/terms lost 'TERMS AND CONDITIONS'; /lab/en/dadada and /lab/es/dadada lost their title heading; DWE1406 and STW3344 render the same image twice, STW3344 and /collect the same player twice; /collect/about, /collect/cards, /collect/decks, /collect/suits, /interplanetary-players, /music, /tree and /tree/max-network-berlin are under 85% of production body text; /tree lost its soundcloud link. NOT fixed here - the migration is MW-7 and owned elsewhere. 7 divergences are recorded as reasoned per-page exclusions in the expectations file instead, each naming the ledger entry that blocked it
2026-07-28T23:51Z  MW-7   DONE     pages/title-casing-and-brand               every <title> was lowercased and stripped of the brand at migration time - About - MAAR WORLD became about on all 95 migrated pages. the site-wide lowercase is a text-transform in reset.css, which cannot reach <title>, so browser tabs, search results, share cards and screen-reader page announcements were lowercase and brandless while <h1> was correct. title: now carries the exact string production served, read from the frozen manifest (MAAR WORLD / COLLECT.MAAR.WORLD / TREE.MAAR.WORLD suffixes), 95 of 95 matched with no fallback. the h1 lead uses the unbranded page name. index members also carry indexLabel so an index row can name the page without the brand
2026-07-28T23:53Z  MW-11  DONE     shell/dark-and-paper                       layouts/shell-dark + layouts/shell-paper per the spec folder map, wired through BaseLayout on the existing surface prop. shell is full-bleed at every breakpoint; the bounded column is .shell__inner (--content-max 1180) and prose is .shell__inner.measure (--measure 66ch) - [...page].astro was at 72ch and is now 66. gutters 20/32/48/64 bind --gutter per breakpoint; 20 is not on the spacing scale so it lands in tokens.css, the only file allowed a raw value. real landmarks header/nav/main/footer, one main owned by the shell so the skip link target cannot go stale. no rule under the header or above the footer - the divider rule permits one in exactly two places and neither is chrome
2026-07-28T23:53Z  MW-11  DONE     shell/area-navigation                      three areas, one pigment role each from AREAS - maar/collect/tree, never a hex. current area is marked three ways because a pigment field never carries meaning alone: the field, an underline, and aria-current. section links per area are the measured legacy header (ledger shell/legacy-nav-inventory), minus the cross-origin links that the area row now carries and minus collect's bandcamp Buy Now. hover inverts to --action-invert on both surfaces - cream on dark, ink on paper, one rule, no hue shift; press is --press-nudge; every target is --hit-min 44px
2026-07-28T23:53Z  MW-11  DONE     shell/mobile-nav-no-js                     zero application javascript added: still 1 of 133 pages references a js asset (/helix-diagram.html) and 0 carry <script src>. the compact menu is a <details> disclosure. the wide nav is NOT that same <details> forced open - whether css can re-show a closed <details> depends on the engine's ::details-content implementation and the failure mode of getting it wrong is a nav that is invisible AND has no control to open it. they are two elements, exactly one display:none at any width, so never a second tab stop, and with no css at all both are simply visible. verified in-browser at 390px: closed panel links report checkVisibility false and focus() leaves activeElement on body; open gives 5 links at 44px, no horizontal scroll
2026-07-28T23:53Z  MW-11  DONE     shell/footer                               exactly the three things the legacy footer carried: instagram.com/maar_world_records as an INLINE svg (the bootstrap bi-instagram path the legacy sites already served, mislabelled telegram.svg to match the mislabelled author.telegram key), a copyright line, and schema.org Person microdata. inline svg costs no per-page image count - an <img> would have moved all 133 counts that verify:content asserts exactly. no privacy link (that url is the cookie-consent dialog body, MW-1 forbids it), no bandcamp, no nav, no heading, no rule. the instagram url is already in the external-links baseline so verify:links reports it as a recorded removal returning, not as an unreviewed introduction
2026-07-28T23:53Z  MW-11  DONE     shell/lang-per-page                        BaseLayout hardcoded <html lang=en> on all 133 pages. lang now threads from the page record: the 10 /lab/es/* articles emit lang=es, verified in dist. a screen reader given the wrong language reads spanish with english phonemes and a translation tool is told there is nothing to translate. the shell is written in english on every page including those, so <header> and <footer> carry lang=en whenever the document is not english - declaring the document es and leaving the chrome undeclared is the same defect one level down
2026-07-28T23:53Z  MW-11  BLOCKED  shell/lang-two-pages-unset                 reason: /esp-feedback and /helix-diagram carry no lang field in frontmatter, so they still emit lang=en. /esp-feedback is a spanish feedback form and /helix-diagram's figcaption is entirely spanish. the fix is one frontmatter line each in src/content/pages/esp-feedback.md and helix-diagram.md - src/content/** is not this session's to touch and two agents are concurrently rewriting it. the layout already threads whatever the record declares, so adding lang: es is the whole change
2026-07-28T23:53Z  MW-11  DONE     marks/legal-position                       all 69 GlyphRun instances rendered inside <p class='t-meta suit'> - a label line, where the rules-of-use table says NO marks, never tilt, one face. zero appeared at h1, where up to 2 are allowed. moved: 69 of 69 now render inside the h1 and 0 in the label line, counted from dist. one run per h1 rather than two, because the responsive table caps an h1 at one mark below 600px and one run then needs no breakpoint branch. aria-hidden inside the heading, so the accessible name stays the card title alone. the label line keeps both its texts as spans separated by gap - the divider rule's 'space does that work' - rather than getting its old middot back
2026-07-28T23:54Z  MW-11  BLOCKED  pages/missing-alt                          reason: all 12 images lacking alt are raw <img> inside markdown BODIES under src/content/**, which this session is explicitly told not to touch and which two agents are rewriting right now (verified: uncommitted edits sitting in src/content/pages/ and verify/ during this run). no template renders them, so there is no place in src/layouts, src/components or src/pages from which to fix them. exact list, all decorative or captioned nearby, so alt='' unless noted - collect/index.md x6: /img/landing/2024_ss-12,-10,-8,-11,-2.jpeg (class lightbox-ignore) and 2024_ss-7.jpeg; radio.md x3: /img/radio/card-banner-3,-2,-1.png (these three ARE meaningful, they name the shows - alt should carry the banner's text); collect/docs/ent-cards.md x1: /img/docs/covers/qr-technology.jpg; cards/STW3344.md x1 and cards/DWE1406.md x1: the dropbox cover, which the card template ALSO renders as <figure class=cover> - production serves 1 image on each and the build serves 2, so the body copy is the duplicate and deleting it fixes both the missing alt and the verify:content image count
2026-07-28T23:54Z  MW-11  NOTE     shell/page-families-outstanding            the spec's folder map names ten page families (home, index, entry, collection, place, tree, search, form, faq, utility) and this session built the shell only. every route still renders family 03 entry. also unimplemented and settled enough to build: the display scale's 40/60 steps (type.css has 76 and 48 only, and the responsive table gives 40/48/60/76 - line-height and tracking for the two new steps are NOT given, which is why they were not guessed), the two-up card grid at 900+, and the button component's four variants. the nav's eventual data source is the inNav field on the page schema, which is false on all 95 migrated pages
2026-07-28T23:54Z  MW-11  NOTE     verify/content-failure-not-the-shell       verify:content FAILs on this run, and it is not this session's. it was rewritten mid-run by a concurrent agent from 95 pages to 130 with 5 assertions. measured both ways against the same scripts and content: WITHOUT the shell 173 problems across 81 of 130 pages, WITH it 151 across 76. the shell strictly improves it because every one of the five assertion kinds is monotone under adding chrome - headings and links are substring presence, minTextLength is a floor, and the shell adds 0 <img> and 0 <iframe>. the residue is theirs: 'soundscapes & music' is asserted against text the check never entity-decodes so it reads soundscapes &amp; music, and /DWE1406 + /STW3344 report 2 images where production has 1 because the card template's cover figure duplicates the body image
2026-07-28T23:55Z  MW-11  NOTE     verify/shell-isolation-remeasured          re-measured back to back against the concurrent agents' latest committed scripts + expectations, after their MW-7 title and content-divergence commits landed mid-session: WITHOUT the shell 166 problems across 81 of 130 pages, WITH it 150 across 80. the absolute numbers move every time they write; the sign does not. verify:build median body text 972 -> 1101 chars is the same effect from the other side. all other checks hold at their pre-shell values: selftest 56/56, schemas 12/12, build 6/6 with 0 warnings and 133 pages, contract 3/3, routes 7/7 with 320 distinct paths and 0 missing, cards 20/20 plus the MW-10 host-canary skip, links unchanged at the same 75 dropbox on-load refs
2026-07-28T23:56Z  MW-7   DONE     lab/index-year-groups                      the article-list include rendered a year heading, a title heading, a formatted date, a 200-char excerpt and a tag list per article; the migration reduced it to indexOf lab and the generic flat ul shipped 1016 chars against production's 5792 - every excerpt and every tag gone. the index is now written into the page body as design-spec page family 02: year-grouped rows with one rule per year group. 20 rows, 4 year groups, h2 4 / h3 20 / p 22 - production exactly. 13 of the 20 articles keep their date in the filename prefix, not in frontmatter, so dateOf reads both - reading only date: had silently dropped the whole of 2024 and 2023. tags are chips not links: production linked each to /lab.html?tag=X and a script hid the non-matching rows, and MW-7 forbids that script, so the link would land on an unfiltered /lab and do nothing
2026-07-28T23:58Z  MW-8   DONE     collect/card-layout-tail                   the 34 collect card records are frontmatter only - every visible word came from the card.collect layout - so the migration emitted 34 pages with an empty <div class=legacy> and lost the whole tail: the Snippet heading, the note on what collecting unlocks, and the player instructions with their support.apple.com NFC help link, the single outbound link on all 34 pages. restored; the bandcamp merch select and Buy button in the same tail stay dropped (MW-8 retires the storefronts, MW-7 forbids its script). production's Please ---- unmute keeps the placeholder dashes, which are not carried. the tail renders above the player because the route puts the body before the snip_player iframe and the route is not this session's to reorder. verify:content problems 106 to 38, zero empty legacy divs anywhere in dist
2026-07-29T07:50Z  MW-7   DONE     lab/disqus-and-continue-reading            removed dead disqus markup + TeXt Continue reading chrome from 8 lab pages; disqus.com removal recorded as reviewed
2026-07-29T08:00Z  MW-11  DONE     pages/missing-alt                          all 12 raw <img> in migrated bodies now carry alt, written by the migration rather than by hand: migrate-pages.mjs gained an IMAGE_ALT table keyed by page-key|src and reports any alt-less <img> that has no recorded decision, so a new one shows up in the migration output instead of shipping silently. collect x6 and collect/docs/ent-cards x1 are decorative (alt=''), radio x3 carry the line of text printed on the ENT card face. the two card bodies (DWE1406, STW3344) duplicated media the card route already renders from the same frontmatter fields, so migrate-cards.mjs now drops any body <img> whose src equals card_image/cover and any body <iframe> whose src equals player/player2/snip_player - which is also what fixed /DWE1406 and /STW3344 serving 2 images where production serves 1, and /STW3344 serving 2 players where production serves 1. verify:content 56 -> 53 problems
2026-07-29T08:07Z  MW-7   DONE     pages/article-title-heading                the legacy theme printed the frontmatter title as the first <h1> on every layout:article page, above whatever the body opened with. the migration only asked whether the body contained an <h1> ANYWHERE, so six pages whose body carries an h1 that is not the title shipped without their title heading: /collect/docs/mw/terms lost TERMS AND CONDITIONS above its body 'Terms and Conditions', /lab/en/dadada and /lab/es/dadada lost 'Dadada (EN)/(ES)' above '4 the dadadaistS' / 'Para lxs Dadadaistas', /privacy and /collect/privacy lost 'Privacy' above 'Privacy and Cookies Policy', /subscribe lost 'Subscribe' above 'Join mail list'. the question is now 'is the body's FIRST heading the page title', and the answer is only acted on when routes/manifest.production.json records the title as production's own first heading - so no page can gain a heading production did not serve. exactly 6 pages changed, each verified against the live page. verify:content 53 -> 50 problems
2026-07-29T08:09Z  MW-7   DONE     pages/commented-out-embed                  a comment is not markup. /collect carries <!-- <iframe ... youtube.com/embed/gpL2sTqXdrA ...> --> - an embed its author switched off, which production never rendered and never requested. the migration matched the tag without asking whether it was live and emitted a facade inside the comment: unclickable, but counted, which is why /collect built 2 embeds where production serves 1. comments containing element markup are now dropped and reported; the 51 prose comments (<!-- Slide 1 -->) are untouched. the youtube URL leaving the page is recorded in routes/external-link-removals.json via links:review-removals --write. dropbox on-load count 75 -> 73 (the two card body covers removed in the previous unit), never up. verify:content 50 -> 49 problems
2026-07-29T08:13Z  MW-7   BLOCKED  pages/ip-orchestra-bruna-image             reason: /lab/en/ip-orchestra and /lab/es/ip-orchestra each reference <img src='/img/about/Bruna.jpeg'> (a round author avatar, legacy sources collections/_lab/{en,es}/2024-08-30-IP_Orchestra.md line 119/135). the file 404s on maar.world AND on collect.maar.world today and exists in no read-only checkout, so production itself serves a broken image there - 12 img elements, 11 images. it is already carried as a per-page images exclusion in the expectations, so nothing asserts it. the only first-party candidate is /img/about/bruna-profile.webp, a DIFFERENT file used by /about; substituting it would put an image on the page that production never served. needs an owner to say whether that substitution is wanted
2026-07-29T08:13Z  MW-7   NOTE     content/residue-is-check-side              all 49 verify:content problems left are the check disagreeing with itself, not content lost. NOT fixed in content, because the content is right. (a) 33 headings 'soundscapes & music' on card pages: verify-content.mjs stripTags() decodes no entities, so the build's text reads 'soundscapes &amp; music' while the expectation, taken from production HTML that carried a bare &, reads 'soundscapes & music'. the label is src/pages/[cardCode].astro line 112 and a reader sees the ampersand correctly; the sibling label 'spoken word' on the same 33 pages passes. emitting a bare & to satisfy it would be invalid HTML. (b) 7 text floors: the production side measures the WHOLE document, so <title> counts as body text, while the build side measures mainContent() only - collect/about prod body 32 = 'About - COLLECT.MAAR.WORLD' 27 + 'About' 5, build 5; decks 66 = 27 + 39, build 39; suits 61 = 27 + 34, build 34; music 175 = title 19 + dropped material-symbols ligature 'nature_people ' 14 + 142, build 142 - four exact identities, nothing missing. tree and tree/max-network-berlin build LONGER than production minus title and ligatures (87 vs 82, 306 vs 301). interplanetary-players' 148 production chars are the escaped literal '&lt;!DOCTYPE html&gt;&lt;html lang=en&gt;&lt;head&gt;' / '&lt;/head&gt;&lt;body&gt;' / '&lt;/body&gt;&lt;/html&gt;' that Jekyll printed as visible text - production ships a broken page and the migrated stub is the fix, not the loss. (c) 8 disqus.com/?ref_noscript links: removed on purpose this session, MW-7 lab/disqus-and-continue-reading. (d) /tree missing soundcloud.com/maarworld: the expectation took it from the STALE ../tree.maar.world/_site/index.html; the frozen routes/manifest.production.json for tree.maar.world/ lists 7 outboundLinks and soundcloud is not among them, live tree.maar.world serves no soundcloud link, and the migration source ../tree.maar.world/index.html has none - the build matches production and the baseline does not. (a) (b) and (d) need author-content-expectations.mjs / verify-content.mjs, which this session is told not to touch
2026-07-29T08:13Z  MW-7   NOTE     content/landings-and-tables-confirmed      re-measured the three defects reported as open at session start, against the live pages. /landings no longer leaks markdown: zero literal '- <a href' bullets, and the list structure now matches production exactly - 15 <ul> and 65 <li> on both sides once theme chrome and <script> are removed. the +20 <p> that remains is CommonMark rendering the same lists loose (<li><p>text</p><ul>...) where kramdown rendered them tight; item count and nesting are identical, so 'list, 20 items' is announced. the two latent bugs named with it are already closed: dedentHtmlBlocks() carries no cross-file depth counter (it matches opens to closes on a per-file stack and reports every unclosed tag with the line it was bounded to) and BLOCK_TAG already lists p, li, td and iframe. the kramdown pipe tables are gone as a class: a scan of all 133 built pages for literal | surviving outside <pre>/<code> finds none in any table position - collect/docs/mw renders 2 <table> with 2 <th> and 27 <td>, collect/docs/releases/skysounds renders 1 <table> with 18 <td>, and the only remaining pipes are a page title, a soundcloud track name and the astro island script
2026-07-29T08:19Z  MW-7   NOTE     docs/content-pipeline-notes                rescued a session handoff written to /var/folders (ephemeral) into docs/agent/CONTENT-PIPELINE-NOTES.md; repo state is the only durable record
2026-07-29T08:25Z  MW-11  BLOCKED  design/page-bodies-unstyled                shell is styled, page BODIES are not: 146 distinct classes in migrated content, 141 with no CSS at all (dead TeXt names: hero x45, swiper__slide x38, button x34, index-row__tag x87). owner must choose: restyle the legacy class names against tokens, or replace them by changing what the migration emits. blocked also on DesignSync being reachable to re-read the spec live
2026-07-29T08:28Z  MW-11  NOTE     docs/wrong-design-skill                    corrected CONTENT-PIPELINE-NOTES: ps-design-lib-first is NOT binding here - it targets plantasia.space-design for orbiters/entangled-worlds, and addendum D1 explicitly rejects adopting that library. authority is Maar World Design System via DesignSync, read live
2026-07-29T08:37Z  MW-11  DONE     verify/a11y-check                          MW-11 lists heading hierarchy, landmarks, keyboard operability, focus visibility, contrast and reduced motion as HUMAN review gates. a human reviewing 133 pages once is a snapshot, not a gate: the next migrate run regenerates every body from the legacy checkouts and nothing would notice a heading level, an iframe title or an alt going missing again. so the mechanical half is now scripts/verify-a11y.mjs - 18 per-page assertions plus 6 stylesheet ones, measured against dist and against the CSS that ships (including inline <style> blocks: the .glyph-run rule that paints --ink-faint is inline on the 35 card pages, and reading only dist/*.css concluded it was painted nowhere). contrast is computed, not transcribed - tokens are resolved out of the built CSS through var() and color-mix(... N%, transparent), composited over their surface, and measured as WCAG relative luminance: 17 pairs, lowest 3.33:1, on the dark (124 pages) and paper (9) surfaces. 24 selftest cases, 57 -> 81, one per assertion plus three that prove the shapes which merely LOOK wrong still pass - a wrapping <label> with no for=, an aria-hidden region that is also display:none, and a decorative alt=''. NOT wired into npm run verify yet: it FAILS on 6 real defect classes, fixed in the units that follow
2026-07-29T08:42Z  MW-11  DONE     a11y/heading-outline                       the legacy Jekyll theme printed the frontmatter title as an <h1> above every body, so no body author ever wrote one - they reached for ### or #### when they wanted smaller type, which is a font size and not a level. 38 of 133 pages therefore skipped at least one: /about announced h1 then h4, /collect/docs/ent-cards h1 h3 h4 h6, /collect/docs/mw h1 then four h4. STW3344 was worse - h1 h3 h3 h2, an outline whose LAST heading is a parent of the two before it. and 7 pages carried two <h1>, the six that MW-7 pages/article-title-heading gave their title back plus /radio, whose body has a second one. a screen-reader user navigating by heading gets no signal that an h4 is a sibling and not the child of an h3 that was never there. fixed in the migration, not in the .md: scripts/lib/headings.mjs normaliseHeadingLevels() walks the body's headings in document order and rewrites each to one level deeper than its nearest smaller-level ancestor, base 2 - so gaps close, siblings stay siblings, and nothing is added, removed, merged or reordered. ATX and raw <h1>-<h6> both, with fenced code, <pre>, <code> and HTML comments masked out by NUL-delimited placeholder so a '#' inside one is never promoted. the page's single <h1> is the materialised title (or the body's own first heading where that IS the title); the 69 card pages take leadsWithTitle:false because src/pages/[cardCode].astro renders their <h1> from card_title. heading TEXT is untouched byte for byte - verify:content asserts text and says nothing about level, correctly, because the level was the theme's decision and the text is the content. 38 pages + 2 card bodies changed, verify:a11y h1 and heading-skip 0/133, verify:content unchanged at 49, verify:build 0 warnings, dropbox on-load unchanged at 73
2026-07-29T08:46Z  MW-11  DONE     a11y/frame-titles                          30 <iframe> across 10 pages shipped with no title. MW-9 correctly left same-site frames as plain iframes rather than facades (play.maar.world and radio.maar.world share the registrable domain), but plain does not mean nameless: a screen reader announces an untitled frame as 'frame' and nothing else, five times in a row on /lab/en/ip-2. the name was already on the page for a sighted reader - every Lab player sits in <div class='container'> with its track name in the block immediately above ('1.Trompeta Mochica.', '104.Dadada Live set Intro 4 IP'). titleUntitledFrames() in migrate-pages.mjs takes that block and emits title='<label> — player', matching the shape [cardCode].astro already uses for the card players. a label is a name and not a sentence, which matters because /radio puts an instruction paragraph in exactly that position ('Press PLAY. Desktop recommended. Due to restrictions set by Apple...') - a candidate is rejected if it runs past 120 chars or contains an internal sentence boundary, so /radio falls through to a per-host name ('maar world radio') and /music's 86-char track title, which has neither, is kept. any frame that falls through to a host name is reported by npm run migrate:pages, and one with no host entry at all is reported as a problem rather than shipped unnamed. ORDERING TRAP, cost 29 wrong titles: this must run AFTER the kramdown {:.class} attribute-list strip. every Lab frame has {:.success} on the line between it and its track name, so titling first read the attribute list as the label - and the strip then deleted it back out of the title attribute it had just written, leaving title=' — player'. verify:a11y frame-title 0/133; verify:content unchanged at 49, links unchanged at 73 dropbox and 213 external
2026-07-29T08:48Z  MW-11  DONE     a11y/emptied-anchors                       /radio and /subscribe each shipped <a href='http://eepurl.com/if7emL' title='Mailchimp - email marketing made easy and fun'></a> - an anchor with no content whatsoever. it is Mailchimp's attribution badge, whose only child was <img src='https://eep.io/...branding_logo_text_light_dtp.svg'>, and defuseThirdParty drops that correctly because nothing third-party may be requested on page load. what was left is zero-size, so it cannot be clicked or tapped, and it is still a tab stop - a keyboard user lands on nothing. it also had just enough of a name to slip past a check that asks only 'is it named': the accname spec falls back to the title attribute, so 'is this link named' was TRUE and 'is this link a link' was false. verify:a11y asserts both separately for that reason. dropEmptiedAnchors() in migrate-pages.mjs removes any <a href> whose content every earlier rule has emptied, plus a wrapper left holding only it, and reports each one. an anchor with its own aria-label is left alone - that is a named control by intent. the title is NOT substituted as visible text: the badge said its words in artwork this build deliberately never fetches, and captioning an image nobody here has seen is a guess about what production showed. the outgoing eepurl.com link is recorded in routes/external-link-removals.json via npm run links:review-removals --write (109 reviewed removals, 108 before). dropbox on-load unchanged at 73, verify:content unchanged at 49
2026-07-29T08:51Z  MW-11  DONE     a11y/emptied-anchors-corrected             CORRECTS the a11y/emptied-anchors line above, which removed the two emptied Mailchimp badge anchors outright and claimed verify:content was unchanged. it was not: deleting them took an outbound link off two pages production serves it on, and verify:content asserts the outbound link set per page against the frozen manifest, so 49 problems became 51. the claim was made from a verify:content run taken BEFORE the change - the exact failure OPERATING-RULES names, declaring work done from judgement instead of from an exit code. the anchor now reads its own title attribute as visible text instead: <a href='http://eepurl.com/if7emL'>Mailchimp - email marketing made easy and fun</a>. that is not an invented caption, it is a string production already served on this element, written by the embed code that placed the badge; the attribute is dropped once it is the text, because a title duplicating its own content is announced twice by some readers and shown as a redundant tooltip to everyone else. an anchor with neither content nor a title has nothing to become and is still removed and reported. verify:content back to 49 across 48 pages, external URLs back to 213, routes/external-link-removals.json reverted to its 108 reviewed removals, dropbox on-load 73, verify:a11y 24/24
2026-07-29T08:51Z  MW-11  DONE     a11y/table-semantics                       two legacy tables shipped with no <th> anywhere, because kramdown accepts a pipe table with no separator line and both used it - so every row was announced without ever saying what a column is. neither has a header row to promote: 'Idea | Maar' in the skysounds credits is data on both sides. what it has is a LABEL column, the left cell naming what the right cell is, which is exactly what <th scope='row'> means - a reader now says 'Idea: Maar' instead of two loose cells. 9 rows marked. /collect/docs/mw's other table is one column holding a single sentence, which kramdown made a table of only because the line began with a pipe; it is not tabular at all and is marked role='presentation' so it reads as the sentence it is rather than being announced as a one-by-one table. these two are the whole population - the corpus has exactly two header-less pipe tables and each was read before the rule was written; a third is reported by the existing pipe-table line. one CSS rule keeps the change invisible: <th> defaults to bold AND centred, and a centred label column is the half of that which would read as a mistake, so .legacy th gets text-align:start and keeps the weight. verify:a11y table-header 0/133; verify:content unchanged at 49
2026-07-29T08:51Z  MW-11  DONE     a11y/word-wrap-and-compose                 MW-11 asks that long Spanish copy and unbroken URLs wrap safely. nothing in the build allowed a word to break: reset.css set box-sizing, margins, lowercase and focus, and img/video/svg max-width, but a TOKEN wider than the viewport is the one overflow none of those reach - the element that is too wide is not the element you would write a media query against, so it pushes the whole document sideways and every line on the page gets a horizontal scrollbar. the legacy bodies carry bare URLs and long compound Spanish words, so it is not hypothetical. body now sets overflow-wrap:break-word - break-word and not anywhere, so a word breaks only when it alone cannot fit and ordinary prose keeps normal line breaking. with that, verify:a11y is 24/24 and is now composed into npm run verify between verify:links and ledger:check, so it is part of the command whose exit code decides. the CI workflow needs no new step - it runs npm run verify, and selftest case F7 already asserts nothing CI runs is missing from CHECKS. npm run verify 70 passed 2 failed 1 skipped, up from 46 passed 2 failed 1 skipped; the two failures are the same two, verify:content at 49 and the 73 dropbox on-load refs, both human-gated. every invariant held: routes 0 missing / 320 distinct paths, cards 20/20, contract 3/3 sha256 3f55727ba0d4, build 0 warnings / 133 pages, selftest 81/81
2026-07-29T08:56Z  MW-11  DONE     design/display-steps-and-buttons           read the live spec through the DesignSync MCP (project 9c177a8a, 'Maar World Design System.dc.html', 113KB) - it was unreachable during MW-9, so this is the first session that could. two of the items the ledger line shell/page-families-outstanding left open are now built, both straight off the spec. (1) DISPLAY SCALE. the spec states the display step in two places that do not cover the same ground: the type-scale table names two steps (76/0.98 and 48/1.02, the second labelled 'h1 below 900px'), the responsive table names FOUR sizes, one per breakpoint - 40 / 48 / 60 / 76. type.css bound only two, so an h1 was 48px at 360 where the table says 40 and 48px at 1024 where it says 60, wrong at half the breakpoints. now mobile-first with one step per query, at the same three widths as shell.css binds --gutter. the two new SIZES are the spec's own; their LINE HEIGHTS are not stated anywhere, so they are interpolated linearly between the two that are, giving 1.03 at 40px and 1.00 at 60px, and tracking is held at -0.02em because both stated steps use it. that interpolation is the only value in tokens.css not read off the spec - see the NOTE line following this one. both sizes clear the 34px bodoni floor. (2) BUTTONS. styles/button.css is the spec's five-by-four state grid transcribed cell by cell - primary / secondary / collect / text across rest / hover / active / focus / disabled. one hover rule serves both surfaces because cream-on-dark and ink-on-paper are the same token (--action-invert), so nothing branches on surface; press is the 1px nudge; disabled is the 10% field and 60% label the spec draws for all four. filled buttons are 11/18 and the outlined one 10/17 plus a 1px border so they occupy the same box - neither pair is on the spacing scale, so both live in tokens.css. min-height --hit-min because the padding alone leaves a 14px label 39px tall. the card pages' 'Open Maar Orbiter' action was a local approximation of the primary button with no hover, no press and no disabled state; it now carries .btn .btn--primary and keeps only its alignment. migrated form controls are styled by ELEMENT (.legacy button[type=submit], input[type=submit], text/email/textarea) because src/content is generated and a class added there is removed by the next migration run - that reaches both Formspree forms and both Mailchimp embeds, whose fields were browser defaults on a dark surface. verify:a11y 24/24 including contrast, verify:build 0 warnings, verify:content unchanged at 49
2026-07-29T08:56Z  MW-11  NOTE     design/display-steps                       OWNER DECISION WANTED, one value. the spec's responsive table gives four display sizes (40/48/60/76, one per breakpoint) but the type-scale table gives line heights for only two of them - 48 -> 1.02 and 76 -> 0.98. the 40px and 60px steps have no stated line height anywhere in 'Maar World Design System.dc.html'. shipped as a linear interpolation between the two stated anchors: 1.03 at 40px, 1.00 at 60px, tracking held at -0.02em because both anchors use it. it is the ONLY value in src/styles/tokens.css that is not read directly off the spec. the previous session declined to guess and left the two steps unbuilt, which meant an h1 rendered 48px at 360 where the table says 40 and 48px at 1024 where it says 60 - wrong at two of four breakpoints, and visible on every page. building them with an interpolated leading is the smaller error and it is reversible in one line each, but the spec should state them. same section of the spec, same question: the type-scale row for --t-h1-sm is labelled 'h1 below 900px', which contradicts the responsive table's separate 40 and 48 steps at <600 and 600-899. the responsive table was followed
2026-07-29T09:07Z  MW-11  DONE     design/measure-alignment                   the prose column was centred in the VIEWPORT while the header and footer sat in the 1180 content column, so at 1745px the chrome started at x=276 and the body text at x=480. every page read as two documents that had never met - a line of chrome, then a column of text floating 200px to the right of it, aligned with nothing. cause: .shell__inner centres its box with margin-inline:auto, and .shell__inner.measure narrowed that SAME box to 66ch, so the auto margins re-centred the narrower box instead of leaving it where the column starts. 'content max 1180, prose measure still 66ch' is two bounds and not one: the column is 1180 and centred, the measure is 66ch and sits at the START of that column. the inline-start margin is now the column's own offset, max(0px, (100% - var(--content-max)) / 2), which is the identical (available - 1180)/2 that margin-inline:auto computes for the header - so the two agree exactly rather than approximately - and the end margin takes the remainder. below 1180 the offset clamps to zero and the gutter alone does the work, unchanged. corroborated by the spec's own header markup, which is a 1180 column with margin:0 auto holding a 720px text block left-aligned inside it. measured in-browser after the change: header, main and footer all start at x=276 on a 1745px viewport, scrollWidth == clientWidth. verify:a11y 24/24, verify:build 0 warnings
2026-07-29T09:15Z  MW-11  DONE     a11y/responsive-sweep                      MW-11's responsive gate, measured in Chrome rather than asserted: 'responsive review at 360, 768, 1024 and 1440 CSS px - no horizontal clipping of navigation, media, PDFs, tables or code'. method: each page loaded into a fixed-width iframe served from dist over localhost and measured as documentElement.scrollWidth vs the iframe width, with every element whose right edge passed the viewport recorded - a real engine doing real layout, not a static proxy. RESULT: 0 pages overflow. 101 pages x 4 widths = 404 measurements plus the remaining 32 at 360, the tightest width - 133 of 133 pages covered, no page skipped silently. the 32 are the collect card twins and the two route-proof scaffolding pages, one template, four of whose instances were in the 4-width set. the harness was proved able to fail before the result was believed: injecting a div 200px wider than the viewport into /index.html at 360 moved scrollWidth 360 -> 560 and the offender was reported. per-breakpoint layout matches the spec's responsive table exactly - gutter 20/32/48/64 and display 40/48/60/76 at 360/768/1024/1440, the wide nav display:none below 900 and the <details> menu display:none at 900 and above so there is never a second tab stop, nav links 44px, and at 1440 the content column starts at (1426-1180)/2 = 123 which is where the header starts. TWO SCOPE LIMITS, both closed rather than assumed. (1) the sweep ran with scripts sandboxed off, so the card pages' Orbiter forward could not navigate the iframe away mid-measurement - which it did on the first attempt, and which is why it is off. that also meant the Helix island did not mount, so /helix-diagram was re-measured separately WITH scripts: the island mounts, and its 640px SVG at a 360px viewport does not overflow the document because .helix__frame carries overflow-x:auto and is genuinely scrollable there (scrollWidth 664 vs clientWidth 308), not clipped. (2) hotlinked dropbox card art makes iframe onload wait on the network, so each load carries a 2.5s cap; every page reported a document, none timed out into a missing measurement
2026-07-29T09:16Z  MW-11  DONE     handoff/regenerated                        HANDOFF.md regenerated at the MW-11 a11y and responsive stop. records the new verify numbers (70 passed 2 failed 1 skipped, up from 46/2/1 - the 24 new passes are verify:a11y, now composed into the suite; the two failures are the same two, both human-gated), what the a11y session changed and where each fix lives, the browser-measured responsive result, the three design units done against the newly-reachable live spec, and - separately, because they are the part an agent cannot close - the five MW-11 human gates that remain: 35 card pages reviewed and three physical cards scanned, four real form submissions to Formspree and Google Forms, ~175 play.maar.world embeds confirmed loading from the new origin, the judgement half of the a11y review (does an alt read well, is a focus ring legible over a photograph, does the reading order make sense), and a Lighthouse comparison whose pre-migration baseline was never captured. two new owner decisions added to the blocked list: the interpolated 40px and 60px display line heights, and the seven unbuilt page families - home is specified as family 01 'one feature card, then three entry cards' and building it means deciding WHICH cards, which is content and moves what verify:content asserts
2026-07-29T09:23Z  MW-11  DONE     design/legacy-body-treatment               closes MW-11 BLOCKED design/page-bodies-unstyled from the concurrent session, whose measurement this confirms: 135 of the 147 distinct classes in the built bodies matched no CSS at all, which is why the shell looked designed and the pages under it did not. that line named two ways out and called the choice the owner's; the owner asked for the design system applied, and it was blocked also on DesignSync being reachable, which it now is. took the FIRST option - restyle the legacy names against the tokens - because the second changes what the migration emits and is a larger, later change that this does not foreclose: every rule keys off a class name and simply stops matching when that name stops being written. src/styles/legacy.css, containment and rhythm only, no invented components, every value a token, no raw hex or size or duration. what it reaches, in the order the audit ranked by page count: the migration's OWN unstyled classes first - card-unlock / card-snippet / card-player-note on 34 card pages, where the instruction to unmute your device read at the same weight as the card description, and the 20 index-row* rows on /lab, which shipped as one uniform wall of text where the spec's page family 02 asks for a scannable year-grouped index (title, meta date, muted excerpt, tag chips as flat fields with dark type, per 'a pastel is always a background, never type'). then the theme's own dead names: hero bands on 10 pages, .container player wrappers on 15 (which never filled the measure, so frames sat at the theme's intrinsic width instead of the column's), the dead twelve-column float grid translated honestly to a single column since the spec stacks below 900 anyway, the swiper slides that no longer slide because MW-7 forbids their script, and the theme's a.button links given the SECONDARY variant - a page with six primary buttons has no primary action. also <hr>, which was rendering as the browser's bevelled 3D groove, now the hairline token: the design forbids ADDING rules, and this is drawing one the content already has. NOTHING IS HIDDEN - verify:content asserts a per-page text floor against production, so a display:none here would be a content deletion wearing a stylesheet. verify:content unchanged at 49 across 48 pages, verify:a11y 24/24 including contrast, verify:build 0 warnings and median body text unchanged at 1178, and all 133 pages re-swept at 360 with 0 horizontal overflow
2026-07-29T09:43Z  MW-11  NOTE     design/visual-language-measured            the owner's objection, in their words: 'we kind of developed a language but it's super hard to transmit... the design so far is not being able to export it correctly', and 'what I see now is super basic, it's like a website that is not nice'. they are right and the gap is in the artifacts. THE SPEC NEVER SPECIFIES WHAT A MARK IS. it gives tokens, type, spacing, states, ten page skeletons and a rules-of-use table saying WHERE marks are allowed (h1 and card cover, max 2, tilt permitted) - and never what one looks like. the language exists in exactly one place, planning/design-references/current-site/Maar World 4a.dc.html, as inline styles on individual spans. so an agent that reads the spec and obeys it builds precisely what this repo now has: right typography, right spacing, right contrast, no character. ADMISSION: the previous units in this session were built without opening 4a - only DESIGN-REFERENCES.md's description of it. measured it properly and wrote docs/agent/VISUAL-LANGUAGE.md: seven marks with 4a's own values - cut word (cream field, dark type, Archivo 600 at font-stretch 110-124%, 11-point torn-paper clip-path, 4 polygon variants), highlighter word, struck word, stamp, opposed chips, tilted actions, hatch plate - plus the observation that carries the whole effect: there is no single tilt reused, there are FOURTEEN distinct angles, nearly all between 0.5 and 1.6 degrees with stamps out at 4. one angle everywhere reads as a skewed page; many small unequal angles read as things placed by hand. three real spec conflicts recorded for the owner (the spec's 4-value tilt set is roughly twice the mockup's angles and has no scatter; 4a's structural off-palette colours are near-misses that should collapse onto tokens but its neon must not; font-stretch is in no type scale). and one DOCUMENTATION BUG now fixed in DESIGN-REFERENCES.md: its table said 4a uses gradient washes and the spec forbids gradients, without distinguishing 4a's LINEAR gradients - hard-stop highlighter strokes and hatch patterns with no diffusion at all - from the radial washes that are genuinely forbidden. an agent reading 'the spec forbids gradients' deletes two of the seven marks. the test is diffusion, not the keyword
2026-07-29T10:04Z  MW-11  DONE     design/type-marks-cut-and-highlight        the first two marks, on h1 only, built by reading 'Maar World 4a.dc.html' directly rather than any description of it - which mattered, because the description was wrong twice. (1) 4a's cut word is NOT 'font: 600 22px Archivo': there are SEVEN of them across three sizes and two faces, and the five set in Archivo run 0.57-0.73 of the display type they sit in, because Archivo's x-height dwarfs Bodoni's and a smaller point size is what makes the two read as one size - it is also what keeps the marked word inside the display line box instead of growing it. shipped at 0.68em. (2) the padding is NOT proportional: 4a runs 7-14px horizontal across type from 12px to 62px, and the spec's responsive section says so in as many words - 'marks scale with their type; their padding does not'. shipped fixed at 8px/2px, the 8 being the spec's own mark padding. TWO MARKS, TWO DIFFERENT AUTHORITIES, and this is the part the previous session could not have got right from a summary. mark.highlight IS specified - the live spec draws it as background:#a9d5e8; color:#100f14; padding:0 8px and says 'flat field, 8px horizontal padding, no radius, ink type. always horizontal. one per heading' - so it is transcribed, and 4a's prettier version (a hard-stop linear-gradient marker stroke that deliberately misses the line box, rotated -0.8deg) is NOT taken: a direction mockup does not overrule a specified component. the CUT WORD is the opposite case - the rules-of-use table has a 'cut words' column allowed at h1 and card cover and the spec never once says what one is - so its mechanics AND values are 4a's: cream field, 11-point torn clip-path in four variants, four angles, Archivo 600. the field is --sf-paper and that is not a coincidence dressed up: 4a's #efe7da IS this system's paper surface and, on dark, its ink. a cut word is the page's own ink used as paper. it deliberately does not invert on a paper route. structure: src/lib/mark.mjs chooses (which word, which variant) and emits class names only, src/styles/mark.css draws and holds every value - the spec's folder map asks for exactly that split, 'mark logic lives in patterns/mark and nowhere else'. one module because a heading is marked in two places: patterns/TypeMark.astro for the card h1 the route builds from card_title, and migrate-pages.mjs for the 59 migrated page h1s, seeded on outputPath. WORD CHOICE IS LONGEST-ELIGIBLE, NOT SEEDED-RANDOM, and that is a deliberate difference from GlyphRun: a glyph carries no meaning so it may be picked freely, but a mark lands on a word a reader sees. checked against 4a's own seven, longest-eligible reproduces four exactly (place, exoplanets, lab, human); a uniform pick lands on 'in', 'the' and 'at' about as often as on the noun. 47 stopwords in two languages, because ten /lab/es articles are Spanish. THREE BUGS FOUND AND FIXED BY RUNNING THE CHECKS, none of which judgement would have caught: (a) hash(seed) % 4 put 25 of 37 cut words into two of four tilt variants and correlated tilt with tear, because FNV-1a's low bits lean on the last byte - GlyphRun already shifts before its modulo and this now does too, with a different shift per axis; (b) an Astro expression between </head> and <body> makes Astro drop the <body> tag outright, and verify:build read 133 of 133 pages as hollow - the area pigment binding moved to <html>; (c) marking part of a token split it across a tag boundary and verify-content's stripTags turns every tag into a space, so 'Skylight.1' became 'Skylight .1' and content went 49 -> 52 on three card pages. a mark now wraps the WHOLE whitespace-delimited token, punctuation included, which is also the more honest reading of 4a - none of its seven cut words is half a word. accessibility: the mark is a span around a word it does not change, so a heading's accessible name is its text unchanged; nothing is echoed and nothing needs aria-hidden. marks are permitted at h1 and card cover and the stylesheet also neutralises them at h2 and below, because a generated body is regenerated by a script and CSS is the cheaper guarantee. below 600px the responsive table allows ONE mark per h1, and a card h1 already carries a glyph run, so h1--marked-glyphs stands the cut word down there and keeps the run - the run is aria-hidden and carries no word, so it is the free one to keep. NEW CONTRAST PAIR, per the instruction that type on a field must be asserted: --pigment-ink on --sf-paper, 4.5:1, covering both surfaces because --sf-paper is not swapped by [data-surface=paper]. contrast pairs 17 -> 19, lowest still 3.33:1. selftest fixture gained --sf-paper for the same reason. npm run verify 70 passed 2 failed 1 skipped, identical to the starting state: routes 0 missing / 320 distinct paths, cards 20/20, contract 3/3, a11y 24/24, build 0 warnings / 133 pages / median body text 1178 unchanged, selftest 81/81, content 49 across 48 pages, dropbox on-load 73. measured in Chrome at 1849px: /index, /lab and /STW3344 all scrollWidth == clientWidth
2026-07-29T10:04Z  MW-11  NOTE     design/mark-tilt-set                       OWNER DECISIONS, five of them, and the two the previous session did not name are the bigger ones. shipped with 4a's values as instructed; every one is a small edit to reverse. (1) TILT VALUES. spec: --tilt-a/b/c/d = -2.5 / -1 / 1.5 / 3, 'four values, no others, chosen per instance and then frozen'. 4a: fourteen distinct angles, of which its seven CUT WORDS use -2, -1.8, -1.6, 1.4 and 1.6. shipped 4a's four most distinct (-2, -1.6, 1.4, 1.6) as --mark-tilt-1..4 in tokens.css, kept at FOUR variants precisely so accepting the spec's set is four value edits and nothing structural. worth the owner knowing before deciding: three of the spec's four are already within 0.5deg of a 4a cut-word angle (-2.5 vs -2, -1 vs -1.6, 1.5 vs 1.4/1.6) and only --tilt-d 3deg sits outside the range 4a ever puts on a word - so the two sets are far closer than 'fourteen angles vs four' suggests, and the real loss from the spec's set is scatter, not steepness. the spec's DISCIPLINE is kept either way: chosen from a seed, frozen, never re-rolled at render. (2) TILT ON A SPAN AT ALL - NOT PREVIOUSLY NAMED, AND STRONGER THAN A VALUE DISAGREEMENT. the live spec says 'tilt applies to a containing block only. a highlighted span is never rotated', and its mark.tilt sample rotates the WHOLE heading run, not a word inside it. 4a rotates the marked span itself, which is the mechanic that makes a cut word read as a pasted clipping rather than a label. read narrowly, 'a highlighted span is never rotated' binds mark.highlight - which the rules-of-use table lists as a different column from 'cut words' - and mark.highlight here is horizontal exactly as specified. that reading is what lets both hold, and it IS a reading: if the owner means the sentence to cover cut words too, the cut word loses its rotation and keeps field, tear, face and scale. (3) FONT-STRETCH IS CURRENTLY UNREPRODUCIBLE, ALSO NOT PREVIOUSLY NAMED. 4a sets font-stretch 110-124% on every Archivo mark and it is part of why a cut word reads as a clipping and not a chip. the installed dependency is @fontsource/archivo, which ships static weight files with NO width axis - checked, no wdth and no font-variation-settings in the css - so font-stretch would silently do nothing. it is therefore NOT in mark.css: a declaration that cannot take effect is worse than an absent one, because the next session reads it as done. getting it needs @fontsource-variable/archivo (wdth 62-125), which is a dependency swap affecting every Archivo glyph on the site - h2, h3, buttons, chips - not just the marks. that is the owner's call and it is the single change that would most narrow the gap to 4a. (4) OFF-PALETTE COLOUR. nothing off-palette shipped. 4a's structural near-misses collapsed onto tokens as recommended: its #141319 mark ink is --c-dark #100f14, and its #efe7da cream field is --sf-paper, which is the same value already. none of 4a's neon (#ff3ec8, #35e8e0, #ffb340) is used. (5) A ONE-WORD HEADING BECOMES AN ALL-OVER FIELD. /lab's h1 is the single word 'lab', so mark.highlight paints the entire heading and it reads closer to a badge than to a marked word - visible in the screenshot for this unit. the spec permits it ('one per heading', no minimum length) and 4a never shows the case because none of its headings is one word. cheapest fixes if the owner dislikes it: prefer the cut word when a heading has only one eligible token, or skip the mark entirely below two tokens. one condition in src/lib/mark.mjs either way
2026-07-29T10:16Z  MW-11  DONE     design/marks-owner-corrections             three corrections from the owner after seeing the first unit, all three implemented. (1) THE STAIRCASE - the owner supplied the REASON the spec only states as a rule, and it changes what the rule means. 'a highlighted span is never rotated' is not a prohibition on rotation, it is the mechanic: a highlight is a marker stroke, a hand drawing one holds the pen LEVEL and moves it along the line, so when the line tilts the strokes do not tilt with it - they stay horizontal and STEP, and a run of them down a tilted heading reads as a staircase. rotating them with their block flattens it back into a plain skewed page, which is the thing the language exists to avoid. so the split is not 'marks may or may not rotate', it is: a highlight is a pen stroke and never rotates, a cut word is a piece of paper someone put down and paper does not land square. implemented as CSS and not as a convention, so it is real before the block-tilt unit rather than a promise: .mark--highlight carries transform:rotate(calc(-1 * var(--tilt, 0deg))), which counter-rotates by exactly whatever tilt its containing block set and leaves it level in the page. unset it is rotate(0deg) and costs nothing. written into docs/agent/VISUAL-LANGUAGE.md with the reason, because the owner asked that it be defined and because a rule without its reason is one the next session 'fixes'. (2) NO HIGHLIGHT ON A ONE-WORD HEADING. the owner: 'it's not nice that we highlight when it's the only one word'. correct - /lab's h1 is the single word 'lab' and mark.highlight painted the whole heading, which read as a badge and not as a marked word. a heading with one eligible token now takes the CUT WORD instead. that is the right way round rather than a dodge: a highlight is a stroke laid over RUNNING TEXT and needs text either side to read as one, while a cut word is a clipping complete on its own - 4a's own 'sky' and 'lab' are exactly that case. distribution moved 34 cut / 23 highlight to 46 / 11, which is also closer to 4a's own seven-to-three. (3) FONT-STRETCH, AND NO NEW TYPEFACE NEEDED. the owner offered to find another similar font with a width axis. none is needed - ARCHIVO ITSELF HAS ONE, the repo had simply installed the static build. @fontsource/archivo ships per-weight static files declaring no width at all, which is why font-stretch was silently inert and why it was left out of the first unit rather than shipped as a declaration that looks done and does nothing. @fontsource-variable/archivo is the SAME typeface - same family, same designer, same OFL licence, same 5.3.0 - declaring font-weight 100 900 and font-stretch 62% 125%, which covers 4a's 110-124 with room either side. swapped in, static package uninstalled so the two are never both served, --face-head is 'Archivo Variable', --mark-cut-width is 118% (the middle of 4a's spread: 110/112/118/120/122/124). confirmed live in Chrome, computed font-stretch 118% on a rendered cut word. COST, stated because it is the reason this is a NOTE and not a free win: the latin subset is 90104 bytes against the static 600's 13820, and latin is the subset essentially every visitor fetches - 76 KB more on every page for one axis used on roughly one word per heading. see the NOTE line that follows. still zero third-party font requests: no googleapis, gstatic, unpkg or cdnjs anywhere in dist. npm run verify 70 passed 2 failed 1 skipped, unchanged: routes 0 missing / 320 distinct paths, cards 20/20, contract 3/3, a11y 24/24 with 19 contrast pairs lowest 3.33:1, build 0 warnings / 133 pages, selftest 81/81, content 49 across 48 pages, dropbox on-load 73. /index and /lab re-measured in Chrome, no horizontal overflow
2026-07-29T10:16Z  MW-11  NOTE     design/archivo-variable-cost               ONE NUMBER THE OWNER SHOULD SEE, since it is the only cost this session added and it is reversible in two lines. swapping @fontsource/archivo (static, weight 600) for @fontsource-variable/archivo (wdth axis) takes the latin Archivo subset from 13820 bytes to 90104 - 76 KB more, roughly 6.5x, on every page, because latin is the subset essentially every visitor fetches. the two other subsets shipped (latin-ext 86240, vietnamese 34464) are unicode-range gated and almost never requested. WHAT IT BUYS: font-stretch 110-124%, which is one of the five things that make a cut word read as a torn clipping rather than as a chip - the other four being the cream field, the torn clip-path, the tilt and the 0.68 scale, all of which cost nothing and all of which already shipped. so this is the smallest of the five contributions carrying the entire byte cost of the unit. it was taken because the owner asked for the issue solved and offered to change TYPEFACE for it, and staying inside Archivo is strictly better than that; but they should know the number rather than discover it. THREE WAYS OUT if 76 KB is judged too much. (a) revert: restore 'import @fontsource/archivo/600.css' and drop --mark-cut-width from mark.css - the cut word keeps field, tear, tilt, scale and face, and loses only width. (b) subset the variable font to latin only and pin the weight axis to 600, keeping wdth - a woff2 in the tens of KB rather than 90, but it needs fonttools/pyftsubset in the build and puts a generated binary in the repo, which breaks the current 'every font comes from an @fontsource package' story. (c) accept it: the site ships no analytics, no cookie banner, no third-party request and exactly one page with JavaScript, so its budget is unusually clean and 76 KB of self-hosted font is a defensible place to spend once. RECOMMENDATION: (c) if the owner wants 4a's language faithfully, (a) if the h2/h3/button rendering matters more than the marks - the variable face is used for ALL Archivo on the site, not just the marks, so this is not a mark-only cost. NOT AN OPTION: a different typeface with a width axis. the spec names four faces and Archivo is one of them; substituting it to get an axis Archivo already has would trade a stated rule for nothing
2026-07-29T14:59Z  MW-11  DONE     arch/authoring-seam-and-route-split        the architecture review's candidates 1, 2 and 5, done as one unit because none of them is useful alone. THE PROBLEM, in the owner's words: 'we don't have a way to write the content'. that was literally true - publishing a Lab post required editing a READ-ONLY legacy checkout, adding a route to a frozen hash-locked contract, running contract:relock (human-gated), re-running a 1760-line migration and re-authoring the expectation set. five steps, three of which an author has no business touching, to add one page. (1) TWO PAGE SOURCES. src/content/pages/ is now src/content/migrated/ - still generated, still wiped by rmSync on every run, still fully re-runnable, which is the property the owner correctly refused to give up before cutover - and src/content/authored/ sits beside it and is written by NO script, ever. content.config.ts globs {migrated,authored}/**\/*.{md,mdx} from one base into the single pages collection, so an authored page is the same record as a migrated one: same schema, same route, same outputPath->URL rule, and [...page].astro cannot tell them apart. that last part is the point - this is a real seam because two adapters sit at it, not one. NOTHING IS FROZEN AND THE MIGRATION KEEPS ITS AUTHORITY: this deliberately does NOT end the migration, which is a cutover decision and not this one. (2) THE FLAT FOLDER IS A TREE. 95 files in one directory carrying a four-level hierarchy in their names - collect__docs__ent-cards__nfc.md - because fileNameFor() turned every / into __. it now splits on / into real directories, so the path in the editor is the URL. depth was already in the data: 22 root, 10 one-deep, 57 two-deep, 6 three-deep. the WITHIN-SEGMENT escaping is deliberately unchanged - anything outside [A-Za-z0-9._-] stays ~<hex>, so a space is still ~20 - because one Collect card outputPath is 'collect/cards/032_-maar-sky-sounds.3-card X ' with an internal AND a trailing space, and a filename ending in a space is mishandled differently by git, Windows and every archive tool. the directories were the problem; the escaping never was. zero URLs moved: outputPath is the load-bearing field and nothing in src/ reads the entry id. (3) THE ROUTE CONTRACT WAS DOING TWO JOBS. routes/manifest.production.json answers 'did anything production served disappear' - right forever - and it was ALSO answering 'may this URL exist at all', which is right only until cutover. a frozen crawl of three sites that are about to be switched off cannot authorise a page written next year, so verify:routes counted every new page as an unbacked extra and failed. the allowed set is now preserved UNION authored. authored routes are not a second frozen file - authoredRoutes() reads outputPath straight out of src/content/authored/**, so a page is authorised BY EXISTING and de-authorised by being deleted, with no second list to keep in step and no relock. migrated records are deliberately NOT read there: they are already authorised by the policy, and reading them would let a migration bug authorise its own output. THE FIDELITY HALF IS UNTOUCHED - backed is still built from the policy alone and a preserved route that stops resolving still fails. PROVED BEFORE IT WAS BELIEVED: wrote src/content/authored/hello-authored.md by hand, npm run build emitted dist/hello-authored.html, verify:routes passed at 134 pages, then deleted it. 2 new selftest cases, 81 -> 83: one that an authored record makes its page pass, and one that an unbacked page STILL FAILS when an authored directory exists - the second exists because the first, written carelessly, passed vacuously (its negative regex matched the PASS line's own assertion name, which contains the failure phrase). docs/AUTHORING.md is the procedure, and it lives in docs/ rather than in the authored directory precisely because that directory is globbed - a README.md there would be parsed as a page and fail schema validation. three scripts followed the move: collect-media.mjs and author-content-expectations.mjs now walk a tree rather than readdir a flat directory, and the latter scans BOTH sources. npm run verify 70 passed 2 failed 1 skipped, unchanged: routes 0 missing / 320 distinct paths, cards 20/20, contract 3/3 (untouched - no relock, the manifest and policy are byte-identical), a11y 24/24, build 0 warnings / 133 pages, selftest 83/83, content 49 across 48 pages, dropbox 73
2026-07-29T15:04Z  MW-11  DONE     arch/lab-index-from-the-collection         the architecture review's candidate 3, and the half of 'a way to write the content' that the authoring seam alone did not give. /lab's index was ~104 lines of literal HTML BAKED INTO THE BODY of lab.md, generated by labIndexHtml() from entries collectLabEntries() read out of the READ-ONLY LEGACY CHECKOUT - not out of the collection. record.indexOf was deliberately dropped for /lab so the route's generic list would not also render. the consequence: a post could be written, could build, could pass every check, and would still be INVISIBLE on /lab until someone added the article to a legacy Jekyll checkout and re-ran a 1760-line migration. writing a post and nobody being able to find it is not a way to publish. it is data now. each lab record carries its own description - derived from the body by exactly the truncate(bodyToText()) the baked index used, so the rows read identically - and src/pages/[...page].astro groups by year and renders the rows. the markup is deliberately the shape production served and verify:content asserts: an h2 per year with the year as its text, an h3 per article, a <time>, the excerpt, the tag chips. measured against the baked output: 4 year groups, 20 h3 titles, 20 excerpts, 87 tag chips - identical counts. the 25 headings verify:content asserts for /lab (Lab, four years, twenty titles) are all still present, and minTextLength 4937 still clears. labIndexHtml and collectLabEntries are deleted, and with them the last reason /lab needed the legacy checkout. the index CSS moved OUT of the .legacy scope in styles/legacy.css - not a widening but because the elements moved: they were legacy class names while the markup came from a migrated body, and they are first-party component names now that the route emits them. PROVED END TO END, and the proof found a real defect in my own instructions: wrote src/content/authored/proof-post.md by hand with date: 2026-08-01, and the build FAILED - unquoted, YAML parses that as a date object and the schema wants a string. docs/AUTHORING.md carried that exact unquoted form in its template, so the owner's first post would have hit it. fixed in both, and the failure mode is a good one: astro exits 1, verify:build goes red and names the file and the line. also confirmed the reverse - with the fixed record the post appears on /lab inside the 2026 group with no script run at all, and verify:routes stays green because the record authorises itself. proof file deleted after. npm run verify 70 passed 2 failed 1 skipped, unchanged: routes 0 missing / 320 distinct paths, cards 20/20, contract 3/3, a11y 24/24, build 0 warnings / 133 pages, selftest 83/83, content 49 across 48 pages, dropbox 73
2026-07-29T15:09Z  MW-11  DONE     arch/i18n-as-a-relation                    the architecture review's candidate 4, and the owner's 'we don't have a way to translate'. BEFORE: nothing in the build knew two pages were translations of each other - no hreflang, no field, no link, no switcher. lang was TWO REGEX LINES over a URL prefix, set on the twenty /lab articles and on nothing else, and BaseLayout silently defaulted the other 75 pages to 'en'. that default is why verify:a11y's 'every page declares its own language' assertion could never fail: the layout guaranteed the attribute was always present, so the check asserted the layout and not the content, and /esp-feedback - a Spanish feedback form - shipped announcing itself as English through a green check. FOUR CHANGES. (1) lang IS NOW REQUIRED BY THE SCHEMA and written by the migration onto all 95 records. the default is still English because the site is written in English; what changed is that it is data rather than a layout fallback, so it is visible in a diff, overridable per page, and a record without it fails the build. 78 en / 11 es (was 10 - /esp-feedback joins them, closing MW-11 BLOCKED shell/lang-two-pages-unset for that page). /helix-diagram is deliberately NOT flipped: its prose is English and only the embedded diagram is Spanish, so the document should not claim the whole page is Spanish - the island declaring its own language is the accurate fix and it is a separate change. (2) translationKey, STORED NOT DERIVED. this is the load-bearing decision: three of the ten Lab pairs have slugs that are translations rather than copies (shared-culture <-> cultura-compartida, music-return-to-playing <-> musica-retorno-al-juego, helix-technical-requirements <-> helix-eac-montevideo-2025), so a path-based rule finds 70% of them and - worse - says nothing at all about the 30% it misses. a stored key is checkable, a derived one fails silently. the migration derives it once, by three rules in order because no single one reaches all eleven pairs: shared source basename (9 pairs, including all three divergent-slug ones, because both halves were authored as _lab/en/<name>.md and _lab/es/<name>.md), then shared final slug (ip-orchestra-design, whose sources are 'Orbital Creation Workshop' and 'Taller de creacion orbital' and share nothing), then a stated EXPLICIT_PAIRS table for /eng-feedback <-> /esp-feedback, which shares neither. a group is a pair ONLY if its members are in different languages - two English pages sharing a basename are not translations, and pairing them would put a switcher on both pointing at the wrong page. result: 11 pairs, every one exactly 2 members. (3) src/lib/translations.mjs - alternatesFor() and languageChoices(), pure and framework-neutral. languageChoices returns [] rather than a single self-entry when there is no translation, because a switcher offering one choice is a label and not a control, and 75 pages are in one language only. (4) THE OUTPUT. every translated page carries a real switcher - a nav landmark, one link per language, each declaring hreflang AND lang so 'espanol' is pronounced as Spanish, current marked by aria-current and not by pigment alone - and <link rel=alternate hreflang> in <head>, SELF-REFERENTIAL INCLUDED, which is the half these sets are most often got wrong. AND THE INERT ONE IS GONE: /lab shipped two buttons pointing at /lab?tag=EN and ?tag=ES, query-string filters on a static build with no JavaScript on that page, so both served the identical unfiltered index - a visible, clickable control that did nothing, which is worse than none because a visitor who wants the Spanish articles clicks it, sees the same page and concludes there are none. stripped in the migration and reported. the capability moved, it did not disappear. 3 new selftest cases, 83 -> 86, and they are the FIRST that exercise a src/ module rather than a script - translations.mjs is pure so it needs no fixture. WHAT THIS DOES NOT DO: it does not create Spanish versions of the 75 English-only pages. that is content and it is the owner's. what it does is make adding one a single file with a translationKey, at which point the switcher, the hreflang and the pairing all appear on both halves with nothing else to update. npm run verify 70 passed 2 failed 1 skipped, unchanged: routes 0 missing / 320 distinct paths, cards 20/20, contract 3/3, a11y 24/24, build 0 warnings / 133 pages, selftest 86/86, content 49 across 48 pages, dropbox 73
2026-07-29T15:18Z  MW-11  DONE     arch/one-html-text-module-and-dead-interfaces the architecture review's candidates 6 and 8. CANDIDATE 6 - FIVE TEXT EXTRACTORS, AND A CORRECTNESS RISK. verify-content stripTags, author-content-expectations stripTags, freeze-routes text, verify-build bodyText and verify-a11y textOf, of which the first three were BYTE-IDENTICAL and kept in step by a comment reading 'Same text extraction verify:content and freeze-routes.mjs use'. that is a claim, not a mechanism, and it mattered in one specific place: freeze-routes computes textSha256 for every production route into the frozen manifest, and author-content-expectations recomputes it to decide whether a legacy _site build reproduces production byte-for-byte and therefore whether a page gets an exact baseline or a weaker whole-page fallback. that comparison is only meaningful if both sides ran the same code, and they ran two separately-maintained regex chains that happened to match. now scripts/lib/html-text.mjs, and all five call sites import from it. THE THREE FORMS ARE DELIBERATELY NOT UNIFIED, which is the part that matters: plainText keeps HTML comments and decodes nothing but &nbsp; (its exact output IS textSha256 in routes/manifest.production.json, so changing it invalidates a contract), bodyText drops comments and turns EVERY entity into a space because it only asks 'did this page render anything at all', readableText drops comments and decodes properly including numeric because verify:a11y asks what a reader would hear. collapsing them would silently move every frozen fingerprint in the repo. the differences are now named in one file instead of being rediscovered, and 3 selftest cases PIN them - one asserting the three forms differ in exactly the documented ways, one pinning plainText's output so a future tidy-up fails loudly, one on mainOf. also folded in: 4 decodeEntities became one decodeAttrEntities (with its &amp;-first ordering preserved and the double-decode hazard written down, because the frozen external-link baseline was recorded with it), 2 mainContent/mainOf became one, and 3 copies of the 32-char fingerprint (verify-cards, selftest, and a 16-char variant) became sha(text, length). BEHAVIOUR IS BYTE-IDENTICAL - verify came back 70/2/1 with content still exactly 49 across 48 pages, which is the whole proof: a refactor of the thing that computes the numbers must not move the numbers. CANDIDATE 8 - INTERFACES NOTHING IMPLEMENTS. three collections, genesis/lab/docs, were declared in content.config.ts with zod schemas, validated by verify:schemas, and read by NOTHING - getCollection is called for exactly cards and pages. they are the pre-migration shape; the migration went another way for a stated reason (one flat pages collection keyed by outputPath is what lets 264 preserved URLs collapse onto 95 records, with kind carrying what a page IS), and nothing recorded that they were superseded rather than unfinished. they also cost three [glob-loader] No files found warnings on EVERY build - now zero. removed, with the reasoning in docs/adr/0001-one-pages-collection.md, which is the first ADR in this repo and exists precisely so a future session does not restore them believing they were forgotten. inNav went too: a boolean on all 95 records, always false, read by nothing, described in SiteHeader as 'the eventual source' for the navigation. and SECTIONS moved from inside src/components/ui/SiteHeader.astro to src/config/site.ts, next to AREAS - it was where it was USED and not where anyone would look, and navigation is configuration rather than markup. the ADR records why nav stays DECLARED rather than derived: a content-derived nav still needs a label and an order per entry, which is exactly the data SECTIONS holds, so deriving it would spread one reviewable list across 95 generated files that migrate-pages wipes on every run. verify:schemas 12 cases still (three collection-specific ones replaced by two asserting the same constraints on pages, plus a new one covering translationKey), selftest 86 -> 89. npm run verify 70 passed 2 failed 1 skipped, unchanged: routes 0 missing / 320 distinct paths, cards 20/20, contract 3/3, a11y 24/24, build 0 warnings, content 49 across 48 pages, dropbox 73
2026-07-29T15:34Z  MW-11  DONE     routes/feed-robots-404-restored            closes three MW-4 BLOCKED lines - routes/syndication, routes/crawler-directive and routes/host-error-page - 18 of the 254 live production URLs the build was silently going to stop answering. these three were picked first because ALL OF THEM FAIL SILENTLY, which is the whole argument for doing them before anything louder: an RSS reader whose feed stops updating logs nothing and complains to nobody, it simply stops hearing from you; a search engine that cannot find a sitemap at the URL it has on file just crawls less; and a missing error page shows the HOST's default, so a mistyped NFC card code - the one thing on this site printed on physical objects - lands on a stranger's 404 instead of yours. none of the three produces a signal anyone would notice. (1) FEED. production serves /feed AND /feed.xml, both 200, both application/xml, on all three origins - the manifest records two files and not a redirect pair, so both are emitted with the identical document, because a subscriber's saved URL is whichever one their reader picked up years ago and there is no way to know which. src/lib/feed.mjs renders it, hand-written rather than @astrojs/rss because it is forty lines of XML and a dependency is forever. contents are the 20 dated Lab articles in both languages, newest first, each with a per-item dc:language so a reader can filter, an isPermaLink=false guid that survives a title change, and the atom:link rel=self every validator wants. NOTE FOR THE HOST: /feed has no extension, so its Content-Type is host configuration in exactly the way the extensionless /CODE card URLs depend on the .html fallback - the body is correct XML either way. (2) ROBOTS + SITEMAP. /robots.txt was absent entirely. it is deliberately permissive: nothing here is Disallow-ed, and the 35 card pages stay noindex-by-meta rather than disallowed ON PURPOSE, because Disallow stops a crawler FETCHING a page and therefore stops it ever seeing the noindex, which is the classic way a page stays in an index forever. /sitemap.xml is the path Search Console is registered against; @astrojs/sitemap emits /sitemap-index.xml, so the build had a sitemap nothing was looking for while the URL every engine already knows returned nothing. shipped as a sitemap INDEX pointing at the integration's own sitemap-0.xml rather than a second copy of the URL list - duplicating the list means two files to keep in step and a chance for them to disagree. (3) 404. an authored content record, src/content/authored/404.md, which makes it THE FIRST REAL USE of the authoring seam built earlier this session: it authorises its own route through authoredRoutes() with no contract change and no relock, which is exactly the property that was missing. noindex, and excluded from the sitemap - listing an error page invites a crawler to index 'page not found'. it names the physical-card case explicitly, because a scanned card landing on a 404 is a defect worth being told about. NEW ASSERTION, because three things that fail silently deserve a check that does not: verify:routes now asserts all five endpoints are still served and names any that are missing. it is deliberately separate from the extras check, which only looks at .html and only asks whether an emitted page is WANTED - this asks the opposite question, is something production served still being served, and nothing else in the suite would notice their removal. RESTORED endpoints are also added to  so a restored page is not then reported as an unbacked extra. THE POLICY IS UNCHANGED: all 18 URLs remain dropKind:unresolved in routes/policy.json and verify:contract is still 3/3 against the same sha256. moving them to preserve changes the contract and needs npm run contract:relock, which is a human step in its own commit. 4 new selftest cases, 89 -> 93: XML escaping does & before < (the double-escape bug), dates are RFC-822 and not ISO-8601 (RSS requires it), the feed is well-formed and drops undated items, and a missing /feed.xml FAILS. the healthy fixture gained the five endpoints, and 404.html in it is a real page with a title and body because verify:build holds it to the same standard as any other page - two selftest cases correctly went red when the assertion landed, which is the assertion working. npm run verify 71 passed 2 failed 1 skipped, up from 70: routes now 8/8, cards 20/20, contract 3/3, a11y 24/24, build 0 warnings, selftest 93/93, content 49 across 48 pages, dropbox 73
2026-07-29T15:34Z  MW-11  NOTE     routes/feed-robots-404-typo                CORRECTS one mangled phrase in the DONE line above. it reads "RESTORED endpoints are also added to  so a restored page is not then reported as an unbacked extra" - the word between "to" and "so" was lost. it should read: added to the backed set in scripts/verify-routes.mjs. cause: the word was wrapped in shell backticks inside an argument to npm run ledger, so zsh ran it as a command substitution and interpolated the empty result. the ledger is append-only by design and is not rewritten, so this is the correction. WORTH KNOWING FOR THE NEXT SESSION: any ledger text containing backticks, $ or unescaped quotes is evaluated by the shell before ledger.mjs ever sees it. use plain words in ledger prose, or a heredoc
2026-07-29T15:55Z  MW-11  DONE     routes/browser-chrome-and-brand-mark       closes MW-4 BLOCKED routes/browser-chrome - 21 live production URLs the build emitted none of, so every already-saved bookmark and home-screen shortcut lost its icon at cutover. another silent one: nobody reports a missing favicon, everybody sees it. all six raster icons are BYTE-IDENTICAL across the three origins (checked by md5), so one set serves the merged site; they are copied into media/shared and layered into the build by assemble-public. THE TWO MANIFESTS WERE NEVER CONFIGURED, which is why restoring them verbatim would have been wrong. site.webmanifest carried an EMPTY name and short_name and ffffff for both theme_color and background_color - a shortcut saved from it shows a blank label and flashes white on launch, against a site whose surface is near-black. browserconfig.xml carried TileColor da532c, a burnt orange that appears nowhere in this design and was never chosen: it is the default realfavicongenerator emits. both are now Astro endpoints carrying the real site name and the surface token. THE ICON IS DYNAMIC NOW, which is what the owner asked for, and it means two concrete things rather than an animation. (1) THE HEADER MARK. the header had NO icon at all - just the words Maar World. the only place the logo existed as a vector was safari-pinned-tab.svg, a single potrace path; extracted into src/components/ui/BrandMark.astro, which paints with currentColor so the caller decides. shell.css binds that to --area-pigment, the SAME custom property the current nav field and a highlighted heading already read, so the mark is sky on Maar, pink on Collect and gold on Tree and the whole chrome agrees about which area you are in without anyone restating a colour. measured in Chrome: on /tree the mark computes rgb(231,201,143), which is --c-tree. the mark and the wordmark are ONE link, not two - a logo beside a name going to the same place is two tab stops and two announcements of one destination - and the mark is aria-hidden so the accessible name stays Maar World. (2) THE FAVICON. /favicon.svg is new, drawn from the same path with the tokens rather than whatever the old generator baked in, and it honours prefers-color-scheme: ink on paper in a light tab strip, sky on near-black in a dark one. a favicon with one baked background is invisible against half the browsers in the world. the raster set still ships for browsers that cannot read it, and link order matters - the SVG is declared LAST because a browser takes the last rel=icon it understands. safari-pinned-tab.svg stays deliberately monochrome: Safari treats it as a silhouette and takes the colour from the mask-icon link tag, which is set to the sky pigment, so a multi-colour file would be flattened and the second colour lost. A REAL HARNESS BUG, found because this change tripped it: verify:links reported 134 broken internal links to /site.webmanifest, a file sitting in dist. HAS_EXTENSION in scripts/lib/routes.mjs was /\.[a-z0-9]{1,8}$/ and .webmanifest is ELEVEN characters, so the URL was treated as an extensionless page and resolved as site.webmanifest.html. widened to 12, and checked against the contract before widening rather than after: the only paths in routes/policy.json with a dot-suffix over 8 characters ARE the webmanifests, so nothing that resolved as a page starts resolving as a file. it had been latent since MW-3 and needed a manifest to exist before anything could hit it. the RESTORED assertion in verify:routes now covers all 11 emitted chrome files. 2 new selftest cases, 93 -> 94, plus the healthy fixture gained the icon set; one case correctly went red when the assertion landed. THE POLICY IS UNCHANGED - all 21 URLs stay dropKind:unresolved and verify:contract is 3/3 against the same sha256. npm run verify 71 passed 2 failed 1 skipped: routes 8/8, cards 20/20, contract 3/3, a11y 24/24, build 0 warnings, selftest 94/94, content 49 across 48 pages, dropbox 73
2026-07-29T16:06Z  MW-11  DONE     handoff/regenerated-at-architecture-stop   HANDOFF.md rewritten, not amended - it described a repo that no longer exists. it still said src/content/pages was a flat 95-file directory, that there were five collections, that verify was 70 passed, and it did not mention that publishing a page was impossible, which was the single most important fact about the repo when this session started. the new one opens with a four-row table of what MOVED, because a returning agent needs to know that before anything else. records: the 8 commits from 4fbb16e to a9269c1 with what each did and why; the current numbers (verify 71/2/1, selftest 94/94, schemas 12/12, routes 8/8, a11y 24/24 with 19 contrast pairs) and the statement that verify:contract is 3/3 against the SAME sha256 it had at session start, since nothing this session touched the manifest or the policy and no relock happened; the two failures with a note that neither is a regression; a next-action list in dependency order with the Dropbox card art first because it is the only red check that is not check-side; and eleven genuinely-open blockers. CORRECTS THE BLOCKED LIST RATHER THAN COPYING IT: npm run ledger status reports 24 blocked, but the ledger is append-only so it still lists five that later work closed - helix-diagram (MW-9 shipped the island), designsync-unavailable (the MCP is reachable and the spec was read live this session), page-bodies-unstyled, pages/missing-alt (a11y passes 133/133), and lang-two-pages-unset which is half-closed. the handoff says so explicitly and suggests fixing ledger status to reconcile them, because a status that overstates open work gets ignored. also carries the traps a fresh session would otherwise rediscover: authored/ must contain nothing but page records because the collection globs it, an unquoted YAML date fails the schema, .astro must be emptied after a content change, ledger prose is evaluated by the shell (backticks lost a word this session), design references live one directory UP and not in this repo, and the card pages forward to Orbiter after 300ms so a browser check navigates away mid-measurement unless it uses STW3344 or DWE1406. tree clean at a9269c1, nothing half-applied
2026-07-29T16:09Z  MW-11  NOTE     design/raw-h1-pages-unmarked               found while proving the dev server hot-reloads: /about and /helix-diagram carry their h1 as RAW HTML in the body, <header class=page-header><h1>About</h1></header>, not as an ATX heading. the migration marks only ATX headings, and markHeadingText refuses any string containing angle brackets on purpose - wrapping a word inside markup it did not write is how you produce mis-nested tags - so those two pages get no type mark. exact population, counted rather than estimated: of 95 migrated records, 58 lead with an ATX heading and 57 of those carry a mark (the 58th has no eligible word, which is correct), 2 carry a raw <h1> and are unmarked, and 35 have no h1 in the body at all because their heading is rendered by the route from card_title and IS marked there by patterns/TypeMark. so the gap is exactly TWO PAGES, one of which - /about - is a page a visitor is likely to reach from the header. NOT FIXED HERE because there are two honest ways and they are not equivalent: either teach the migration to unwrap a raw <h1> into an ATX heading, which changes the emitted markup on two pages and touches the heading normaliser, or teach markHeadingText to mark inside a known-simple <h1>...</h1> whose content is plain text, which is a narrower change but weakens the refuse-on-markup rule that exists for a reason. both are small; neither is free; and this was discovered while testing something else, so it is recorded rather than done. verify unchanged, tree clean
2026-07-29T16:28Z  MW-11  DONE     ledger/capped-and-searchable               onboarding cost 24.4k tokens before any work; 11.5k of it was "read the last 14 ledger entries". mean entry across 164 is 753 chars, last session averaged 3222. ledger.mjs now HARD-CAPS detail at 500 chars and adds `ledger -- find <term>` so it is queried, not read. HANDOFF.md cut 4164 -> ~1300 tokens. rules updated
2026-07-29T16:41Z  MW-11  BLOCKED  design/tilted-actions-vs-ui-row            the live spec forbids this mark at the level it lands. rules-of-use row: body, ui, labels, captions - marks none, TILT NEVER. a button is ui, and the spec rotates no control anywhere - all 5 real rotate uses are heading level. but the spec contradicts itself: its tilt-set prose says tilt applies to a containing block only, and a button IS a containing block. 4a tilts 8 CTAs: primary -0.6 -0.7 -0.8 -1, secondary +0.5 +0.6, a pair leaning apart. OWNER DECIDES. see src/styles/button.css
2026-07-29T16:55Z  MW-11  DONE     design/type-mark-stamp                     PLACEMENT MOVED: VISUAL-LANGUAGE.md asks for the stamp on the card suit/number line - a LABEL line, where the rules-of-use table allows no marks. same rule that moved the glyph runs into the h1. it goes to the card cover, which allows 2 marks plus tilt and was spending none; 4b stamps that corner too. 67 covers, text is the roman numeral already in card_title, WildCard has none and gets none. letters are ink-meta not 4a gold - a pastel is never type. see patterns/Stamp.astro
2026-07-29T17:07Z  MW-11  DONE     design/type-mark-hatch-plate               mark 7, and the odd one of the seven: it is a SURFACE, not a mark laid on a word, so it spends nothing from the h1 mark budget and is allowed wherever media is. two flat bands, 8px diagonal, hard stops, behind every media frame - 79 plate elements plus 17 click-to-load facades in dist. bands are --sf-raised and --sf-sunken rather than 4a hexes: the separation is what carries the effect and it survives the collapse, and roles invert with the surface for free. see --plate in tokens.css
2026-07-29T17:13Z  MW-11  DONE     design/type-mark-struck-word               mark 3, the only one that cannot be applied by rule. spec section 02 opens with marks never indicate state or meaning, and a cross-out means something - so nothing chooses a word to strike. it goes only where the COPY already says the thing and the bar adds no information, the standing of the spec own mark.face italic swap. that is ONE heading on the site, the 404: this page is not in the deck. drawn as ::after, so it is not in the a11y tree and needs no aria-hidden. see mark.css
2026-07-29T17:16Z  MW-11  DONE     design/type-mark-opposed-chips             mark 5, HALF SHIPPED and the other half held deliberately. the fill-against-outline pair is the spec own chip table - rest is 1px ink at 20 percent, selected is a field - and 4a en/es is that table drawn in the language switcher. before this a non-current language was plain meta text and the pair did not read as two chips at all. the OPPOSITION is the tilt, and a chip is ui, where the rules-of-use table allows no tilt ever - the same open decision as mark 6. see lang-switch in pages
2026-07-29T17:41Z  MW-11  NOTE     design/tilt-rule-scope                     SUPERSEDES the BLOCKED line design/tilted-actions-vs-ui-row. the owner corrected the reading: I only asked to not tilt the highlighted text, with the tilt it looks bad, it looks like a star. so the no-rotation rule was only ever about mark.highlight, and the rules-of-use row body ui labels captions tilt never is NOT a ban on tilting a control. the spec own tilt-set prose agrees: tilt applies to a containing block only, and a button IS one. highlights stay level, measured 0 deg on all 18
2026-07-29T17:41Z  MW-11  DONE     design/type-marks-tilted-actions-and-opposition marks 6 and 5 finished together, since one decision gated both. 4a values and 4a pattern: filled leans negative (primary -0.6 collect -0.8), outlined leans positive (secondary +0.5), so a stacked pair leans APART. text button stays square - 4a never tilts an action with no box. chips are steeper as 4a has them, selected -1.5 against rest +1, clamped to tilt-b below 600px. tilt is a custom property because :active also transforms and would straighten the press. measured in chrome, 0 overflow
2026-07-29T18:02Z  MW-11  DONE     design/prose-vs-legacy-split               ROOT FIX, not the h1 margin. the owner: we need root fixes, we do not need patches. the h1 had no spacing because the vertical rhythm of EVERY page body lived in legacy.css under a .legacy wrapper - a name meaning temporary on something permanent, so nobody audited it and h1 was simply missing from the list of headings styled. split by lifetime: prose.css permanent, legacy.css disposable and keyed only to dead theme class names. wrapper renamed to .prose. 96 bodies, authored included
2026-07-29T18:02Z  MW-11  DONE     verify/prose-coverage-check                the check that makes the h1 bug unrepeatable, and the reason verify went 71 to 72 - one assertion ADDED, none changed. verify:build now reads .prose tag rules out of prose.css, reads every rendered body in dist, and fails on any element that has neither a rule nor an entry in PROSE_EXEMPT stating why it needs none. 45 distinct elements, 21 styled, 24 exempt. 3 selftest cases: missing h1 rule fails, present passes, and markup inside a script tag is not an element. selftest 96 to 99
2026-07-29T18:19Z  MW-11  DONE     design/spec-tilt-column-corrected          the spec table now matches the owner rule and the repo. rules-of-use row body/ui/labels/captions is split in two: body, labels, captions keeps tilt never; ui - buttons, chips gets tilt ALLOWED. the tilt-set prose now carries the reason rather than the bare prohibition - a highlighted span is never rotated because a highlight is a pen stroke and a hand holds the pen level, everything else that tilts is an object someone placed. written back via DesignSync, two changes and nothing else touched
2026-07-29T18:30Z  MW-11  DONE     verify/mark-class-coverage                 found by auditing for other instances of the h1 bug; the only real one. mark.mjs builds class names by interpolation - mark--tilt-N, mark--tear-N - and mark.css defines them one at a time. they agreed and nothing asserted it. the tilt set is an OPEN decision, so the likeliest edit here changes that count: raise TILTS to 5 and a fifth of every cut word renders flat, silently, since text, hollowness and contrast stay green. verify:build now fails on a rendered mark class with no rule. 72 to 73
2026-07-29T18:38Z  MW-11  DONE     dev/host-url-semantics                     NEVER intermittent, despite the symptom. dev returned 404 for / and /collect and /tree - home plus both area hubs - while preview returned 200 for all three every time; which server was running decided which you saw. cause: those 3 pages have outputPath ending in index, a static host resolves /collect to collect/index.html, dev matches route params literally. fixed by a dev-only vite plugin in astro.config.mjs, apply serve, so it cannot touch a build. was a TRAP; a documented footgun is a patch
2026-07-29T18:41Z  MW-11  NOTE     design/page-families-are-the-gap           the owner list - missing cards, carousels, a hero, content narrower than the header, images that do not fit - is ONE cause not five. / renders family 03 entry, whose spec is single measure 66ch max: an article layout on the home page. the narrow column is family 03 behaving correctly on the wrong page, so do NOT widen the measure. order: patterns/card first since families 01 04 05 07 are all card grids, then ui/carousel, then family 01 home. see Next action in HANDOFF.md
2026-07-29T19:22Z  MW-11  DONE     design/patterns-card                       spec section 04, five variants one anatomy, transcribed: entry compact place collection feature, plus rest hover focus pressed unavailable. first consumer is the two covered indexes - 34 cards on /collect/cards, 10 on /collect/documentation. THE WHOLE CARD IS ONE LINK TARGET, so the old pattern of a link on the thumb AND a link on the title is gone: 34 anchors for 34 cards, was 68. cover is the missing thumbnail, never card_image, so dropbox on-load stays at 73. see src/styles/card.css
2026-07-29T19:22Z  MW-11  NOTE     design/card-badge-scale                    the spec draws badges twice and the two disagree. section 03 badges: DM Mono 12px, filled padding 5px 10px, outline 1px currentColor 4px 9px. section 04 card samples: 11.5px, 4px 9px, 3px 8px. half a pixel and one step of padding apart. shipped section 03 because it is the section that SPECIFIES badges, and two near-identical badge scales in one build is worse than either. one edit in card.css to switch if the owner prefers the card drawing
2026-07-29T19:33Z  MW-11  DONE     ui/carousel                                spec section 06. the dead theme swiper rebuilt as a real carousel BY THE MIGRATION, not by CSS: the a11y section asks for a labelled group with aria-roledescription carousel, slides as list items and a counter in a polite live region, and CSS cannot add a role or a list. 8 carousels, 38 slides across 5 pages - the home page alone had 11 photographs stacked one under another. zero scripts on the page: native scroll-snap plus numbered in-page links. see scripts/lib/carousel.mjs
2026-07-29T19:34Z  MW-11  NOTE     ui/carousel-no-js                          THE ONE TRADE-OFF. the spec asks for the counter in a polite live region, implying it follows the current slide. nothing follows scroll position without javascript, and MW-7 allows application js on one page only. so the counter states the TOTAL - 11 slides - and never changes; the live region stays correct because with no auto-advance there is nothing to announce. all else is native: swipe, trackpad, arrow keys, numbered links. a live 3-of-11 counter is the only part that costs the no-js rule
2026-07-30T05:04Z  MW-11  DONE     ui/carousel-corrected                      the owner rejected v1 outright: numbers everywhere, no photo looking good, not respecting any size. both faults real. (1) FIVE of the eleven home photographs are SQUARE, six are 3:2 - one forced 3:2 frame with object-fit cover cropped a third off every square one. the frame now fixes HEIGHT only, width follows the image, nothing cropped, slides still share a baseline. (2) the numbered control row and on-page counter are gone; the count lives in the accessible name. see src/styles/carousel.css
2026-07-30T07:58Z  MW-11  DONE     design/page-family-01-home                 the home page stops being an article. / rendered family 03 entry - single measure 66ch - which is correct for a document and wrong for a landing; every symptom the owner listed followed from that one substitution, so the fix is a family and not a width. one feature card then three entry cards, composed from patterns/card, no new hero component. dispatch is a schema enum family, not a URL comparison, since nine more skeletons are specified. see src/components/families/Home.astro
2026-07-30T07:58Z  MW-11  DONE     design/home-content-is-fields              family 01 needs props, so lib/home-family.mjs READS the pieces out of the dead theme markup rather than transcribing them: hero--dark becomes card.feature, three wip-card blocks become card.entry, and each region is cut from the body so nothing renders twice. extraction not transcription because a typed-out copy is the correspondence-by-hand defect this repo shipped three times; every region it expects also reports itself missing. verify unchanged at 73 pass 2 fail 1 skip
2026-07-30T07:59Z  MW-11  DONE     design/card-feature-first-render           card.feature had never been rendered by anything and did not lay out. the stylesheet declared a two-column grid over the five FLAT slots every variant shares, so the cover took cell one, the badges took cell two and the title dropped under the image. Card.astro now wraps the non-cover slots in card__body for this variant only - a Fragment for the other four, so the twenty entry cards already shipped are byte-identical. see src/styles/card.css
2026-07-30T07:59Z  MW-11  NOTE     design/feature-excerpt-unclamped           the one place patterns/card departs from excerpt two lines truncated with an ellipsis. the clamp buys a common height for cards sitting beside each other in a grid; card.feature is one per page full measure with nothing to line up with, so it bought nothing and cost the sentence - the home hero ended transform the... and no other part of the page carried it. clamp removed for this variant only
2026-07-30T08:09Z  MW-11  DONE     ui/carousel-prose-containment-collision    THE CAROUSEL BUG WAS THE CASCADE, NOT THE MARKUP. carousel.css says the frame fixes height only and width follows the image - that rule has never applied on any page. prose.css .prose img sets max-width 100 percent and height auto at (0,1,1), exactly the specificity of .carousel__frame img, and it imports later. every carousel sits in a .prose body, so every one lost: on the home page a 1042x695 photo in a 340px frame, one slide filling the track, no peek. now at two classes
2026-07-30T08:09Z  MW-11  NOTE     design/prose-containment-is-general-hazard prose.css containment is element-keyed, so ANY component owning its own media collides with it at equal specificity and source order decides. fixed locally in carousel.css rather than globally: lowering that block to :where() would drop it to (0,0,1) and unmask every other override at once, not a change to make while shipping a page. the next ui component with media should expect this
2026-07-30T08:09Z  MW-11  DONE     ui/carousel-embla                          MW-7 AMENDED BY THE OWNER, second exception after the helix island. two no-script attempts failed for one structural reason: section 06 wants prev and next disabled not hidden at the ends, arrow keys, home and end, and the counter in a polite live region, and CSS builds none of them. engine is Embla, the plain-JS core of the library shadcn/ui wraps - no React, no Tailwind, bundled by Vite from node_modules so nothing third-party fires on load. 19.9 kB, 8.1 kB gzip, 5 of 134 pages
2026-07-30T08:09Z  MW-11  NOTE     ui/carousel-enhancement-not-replacement    the HTML still ships the no-script carousel - native scroll-snap that swipes and scrolls - and the script adds .carousel--live only once Embla owns the track, so a page with the bundle blocked still has a working gallery. the count is announced and never painted: the owner rejected eleven numbered boxes with we have numbers everywhere, and printing 3 of 10 is that mistake in smaller type. slides re-measure on image load
2026-07-30T08:33Z  MW-11  DONE     ui/carousel-loop-dots-and-centre-tween     the owner asked for the predefined embla behaviour: loop, one circle per slide, the centre slide scaled up and lit while the sides dim, variable widths kept. all four in CarouselScript.astro. the tween writes two custom properties per slide from scroll position and carousel.css owns the values - fade floor 0.35, scale floor 0.82 - and it is suppressed entirely under prefers-reduced-motion. the loop correction via loopPoints is not optional or a slide flashes dark as it wraps
2026-07-30T08:33Z  MW-11  NOTE     ui/carousel-loop-supersedes-disabled       section 06 says at the first or last slide the matching control is disabled not hidden. a looping track has no first or last, so there is no end for a control to be disabled at and both buttons stay live. the dots carry position instead, with aria-current on the selected one so the fill is not the only carrier. deliberate, on the owner ask for loop mode - not an oversight
2026-07-30T08:33Z  MW-11  DONE     ui/carousel-video-frame-height-chain       the videos were cut and the images were not, for one reason: an img is a direct child of .carousel__frame and gets height 100 percent of its 340px, but the legacy body wraps every video in a .video-frame div that has no rule anywhere, so it is auto-height and a percentage against auto resolves to auto - the 640x640 videos rendered full size and overflowed. the frame is a flex container now and stretches its direct child, so the chain holds at any depth. videos are 340x340
2026-07-30T08:33Z  MW-11  NOTE     ui/carousel-dots-below-600                 ten dots at the full 44px target plus both buttons is 472px of controls inside a 350px column. below 600 the controls row wraps: prev and next hold the ends of the first line, the dots take their own line centred, and a dot narrows to 32px so all ten fit at 390. the target stays 44px tall - under the spec 44 in one axis, above the WCAG 2.2 minimum of 24x24 in both. two ragged rows of circles read as a fault
2026-07-30T16:28Z  MW-9   DONE     embeds/consent-gate                        clears the MW-9 BLOCKED embeds/click-out-not-load. owner took the third application-js exception 2026-07-30; ui/embed-consent is it. a press builds a youtube/vimeo/soundcloud player in place; the iframe is not in the emitted html, so verify:links still reports the same 73 dropbox refs and no more. reads the provider url off the anchor the migration wrote - no content file changed, baseline unmoved at 213. youtube-nocookie, vimeo dnt=1, no-referrer, nothing persisted. inlined on 9 of 134 pages
2026-07-30T16:28Z  MW-9   DONE     privacy/copy-matches-build                 both privacy pages claimed google analytics, cookies, a cookie chooser (choose which cookies to accept) and youtube collecting data on-site. none existed: dist has zero gtag/GA, zero document.cookie/localStorage/sessionStorage, zero third-party iframes. the policy documented a consent ui never built - likeliest source of the reported unfinished-cookies impression. rewritten to state what the build does, incl. the dropbox artwork and the orbiter card forward
2026-07-30T18:09Z  MW-9   DONE     embeds/consent-gate-poster                 gate is now a poster + play mark + one line, replacing a chip, two links and a two-line note inside a bordered plate nested in the figure. 7 youtube posters self-hosted to media/shared/img/embed-posters (700kb), answering the MW-9 embeds/no-poster note - that objection was that a poster could only come from the provider, and our own copy is not. injected at runtime, not into markdown, because verify:content asserts an exact image count per page. audio gets a strip, not a 16:9 hole
2026-07-30T18:09Z  MW-9   DONE     embeds/referer-error-153                   referrerpolicy was no-referrer, which broke playback: youtube's embedder terms REQUIRE a http referer and a player without one refuses with error 153. now strict-origin-when-cross-origin - the provider gets the origin, never the page path. same header is what vimeo checks. separately: both vimeo videos (252728417, 235986660) are 403 from every referer and render 'sorry, this video is unavailable' - embedding is off at the vimeo end, owner is fixing it there
2026-07-30T18:21Z  MW-9   DONE     pages/feedback-retired                     both were a google form with out-of-date content. retired as 200 + instant meta-refresh stubs to /bookings - the house pattern already used by /interplanetary-players, since a static host cannot serve a true 301 and production retires that url the same way. routes stay preserve, so verify:routes and verify:contract need no relock. noindex + out of the sitemap; content-expectations embeds 1->0 on both with a reason; the 2 google forms urls recorded in external-link-removals
2026-07-30T18:27Z  MW-9   DONE     content/videos-added                       owner's request: luminiscencia litoralena (vimeo 137703428) + tembey (vimeo 104399616) on /landings, orbits and bodies@ircam (youtube jaPHJNyjZ1s) on lab/en + lab/es. all three verified playing in-page. posters self-hosted incl. the first vimeo ones - their oembed answers 200 where the two older vimeo videos 404. the gate now drops a poster img that fails to load, so posterless videos fall back to a plain play surface, not a broken image. 3 urls recorded in the baseline allowedNew
2026-07-30T18:35Z  MW-9   DONE     embeds/poster-play-contrast                the play mark is --sf-paper over someone else's photograph, and the two new vimeo posters are bright: measured at the centre of each frame the tembey still gave 1.92:1 and luminiscencia 2.16:1, both under the 3:1 a non-text control needs. a drop-shadow alone did not carry them. the disc now has a 55% black scrim, which puts the worst poster at 6.98:1 and the set at 6.98-17.1:1. contrast is now a property of the component, not of whichever frame the provider picked
2026-07-30T18:54Z  MW-11  DONE     collage/one-per-section                    the header took a pool of 4 and a script picked one per visit, so 6 surfaces showed the same 4 pictures as each other and a different one to the same visitor each reload - a section header that cannot make a section recognisable. now SECTION_COLLAGE keys one picture to each of 7 surfaces. a fixed choice is known at build time, so the src is in the emitted html and CollageField ships no javascript and needs no noscript twin. 12 collages added, 01-cosmic-botanist + 08-card-oracle removed
2026-07-30T18:54Z  MW-9   DONE     embeds/calendar-gated                      calendar embeds in the page on a press instead of sending the visitor to google. gated like the media, deliberately: the page is only given to people the owner knows, but loading it unasked would still be a third-party request on page load, which is what the whole no-cookie-banner arrangement rests on not happening. verify:links unmoved, 0 iframes in the emitted html. it gets a labelled bar, not a poster - a play triangle on a calendar promises a video that never arrives
2026-07-30T19:10Z  MW-11  DONE     tree/link-in-bio-family                    tree is a link-in-bio page and now has its own family. it was 4 anchors nested inside 4 div.button-container wrappers spaced with br tags - not editable by anyone. links are now TREE_LINKS in config/site.ts, one line each, with material symbols inlined from node_modules as svg (never the icon font, never a ligature - the shell/legacy-nav-inventory trap). 16-cabinet-of-signals is 1080x1920 so it is used vertically: fixed, covering, behind the column. no javascript
2026-07-30T19:10Z  MW-8   NOTE     tree/image-self-hosted                     partially addresses MW-8 tree/sunflower-image (BLOCKED): the tree page rendered no image at all because production's is hotlinked from herbarium.plantasia.space and the file is in no read-only checkout. it now carries a self-hosted picture and makes no third-party request. the original sunflower asset is still unavailable, so the blocked line is answered in effect and not in kind - if that exact image is wanted, it still needs the file
2026-07-30T19:10Z  MW-11  DONE     collage/reassigned                         home 21-celestial-publication -> 03-ritual-interface, lab 16-cabinet-of-signals -> 25-botanical-turntable, bookings 15-folded-present -> 10-orbital-rehearsal, and /collect gains 20-museum-of-cards. 8 surfaces now carry a fixed picture. 16-cabinet-of-signals moves to the tree hub as a full-page vertical backdrop rather than a cropped wide banner
2026-07-30T19:22Z  MW-11  DONE     tree/spacing-and-fold                      the buttons had no horizontal padding and the title no gap because --s-5 and --s-10 do not exist on the spacing scale (1,2,3,4,6,8,12,16,24) and an undefined custom property invalidates the whole declaration. real tokens, content centred in each pill, and the page no longer forces 100svh below a 128px header - that made every visit open scrolled past the last button. labels stay on one line down to 22rem, then the type gives way rather than the layout
2026-07-30T19:32Z  MW-11  DONE     collage/flush-to-header                    the collage started 64px below the header because .page carries a 64px top padding and the hero is its first child. the strip of background above read as a figure that had slipped down rather than the page's opening. .page--hero zeroes only the top; the bottom padding still separates the last block from the footer. home was already flush - it uses .home, not .page
2026-07-30T20:03Z  MW-6   DONE     cards/dropbox-third-party                  clears the BLOCKED line. owner approved self-hosting 2026-07-30. all 37 on-load card images were www.dropbox.com/...?raw=1 - 2.35 MB each behind a 302 chain, 73 refs across 71 pages, the sole verify:links failure. 68 files (34 card faces + 34 thumbnails) downloaded and re-encoded to webp q82 at native size into media/collect/img/cards: largest 187 kB, was 2.35 MB, 2.65 MB for the set, which also answers media/assets-over-2mb. verify:links now PASS 4/0, no third-party request on load.
2026-07-30T20:03Z  MW-8   DONE     collect/cards-covers                       clears the BLOCKED line - the thumbnails were never a code problem, they were waiting on somewhere to host them. the 34 migrated collect-card records carried card_image and no cover, so the covered index grid drew 34 hatch plates. each now carries cover: /img/cards/Thumb_*.webp, mapped from the NFC record pairing the same card_image. order matches production exactly, checked against live collect.maar.world/cards. content-expectations images 0 -> 34 and the exclusion removed, not zeroed.
2026-07-30T20:04Z  MW-6   NOTE     cards/first-party-enforced                 the hotlinks arrived by pasting a dropbox url into frontmatter and nothing stopped that: cover and card_image were z.string().url(), which accepts any absolute url. both fields on both schemas now take the same root-relative /img|/assets regex the migrated-page cover has always used, so a reinstated hotlink is a build failure, not a code-review note. two cases assert it in each direction; check-schemas 13 -> 15. see src/content/schemas.mjs imagePath.
2026-07-30T20:04Z  MW-6   BLOCKED  lab/dead-sponsor-logos                     2 of the 37 could not be self-hosted: the MMAT and UArtes sponsor logos on lab/en + lab/es ip-orchestra. both dropbox scl/fi links answer 'File Deleted' - they are broken images on production today, not just here, so there is nothing to download. left as text credits in .logo-grid; the uartes.edu.ec link is unchanged, which verify:content asserts. content-expectations images 11 -> 9 on both pages with the reason. NEEDS THE TWO IMAGE FILES FROM THE OWNER to restore.
2026-07-30T20:04Z  MW-6   NOTE     cards/js-forward-still-hides-them          self-hosting does not put these images in front of a js visitor. 33 of the 35 card pages carry an inline script that forwards to orbiter.plantasia.space after 300ms, so the page leaves before any image arrives, wherever it is hosted. this change helps the no-js path, crawlers and the third-party gate. whether that forward stays is the owner's call and is not decided here. verify:links does not scan inline js for navigation.
2026-07-31T15:58Z  MW-6   DONE     lab/uartes-logo-restored                   owner supplied the UArtes mark 2026-07-31 (oz-logo-vertical-para-fondo-negro.png), so the dead dropbox hotlink on lab/en + lab/es ip-orchestra is a self-hosted image again: media/maar/img/logos/uartes.webp, 2.8 kB, alpha kept. content-expectations images 9 -> 10 on both, exclusion 3 -> 2. SOURCE IS 110x112 - it is sharp at the size the logo grid draws it and will not survive being scaled up; a larger original is worth asking for.
2026-07-31T15:58Z  MW-6   BLOCKED  lab/mmat-logo-still-dead                   narrows the lab/dead-sponsor-logos line to one file. MMAT-BLANCO-2024.png is still 'File Deleted' at dropbox and broken on production too, so it stays a text credit in the logo grid. needs the image from the owner; drop it in media/maar/img/logos/ and take the per-page image exclusion from 2 to 1.
2026-07-31T15:59Z  MW-11  DONE     design/card-cover-is-portrait              the owner: we present all the cards as horizontal while the images are vertical, so they are all cut. correct - .card__cover is 3:2 with object-fit cover and a card face is 1219x1717, so 58% of every preview was thrown away and what survived was the suit wordmark. patterns/card takes a coverShape prop (band | card); card takes the artwork's own ratio and CONTAINS instead of cropping. the index page declares it via indexCoverShape, never a URL comparison.
2026-07-31T15:59Z  MW-11  DONE     design/card-page-face-uncropped            same crop on both card routes: .cover img was 3:2 + object-fit cover, so /collect/cards/012 showed a strip of SKYSOUNDS.2 and no artwork. one card face has nothing beside it to align with, so it now has no imposed ratio at all - height auto, which also suits the square Stoney Way face. capped at 24rem and centred: at full measure a portrait card is 1.41x the column and buried the page under itself.
2026-07-31T15:59Z  MW-9   DONE     embeds/player-scrollbar                    the snippet player drew a scrollbar down its right edge and along its bottom on every card page. the overflow is exactly 14px per axis - the scrollbar's own width - because play.maar.world sizes its p5 canvas to window.innerWidth while the document clientWidth is 14px narrower once a bar exists, so each keeps the other alive. measured at 1084x1084: scrollWidth 1273 vs clientWidth 1259. both routes now set scrolling=no; the 14px hidden IS the gutter, not content.
2026-07-31T15:59Z  MW-9   NOTE     embeds/player-canvas-is-upstream           the scrollbar fix above is a patch applied from outside the frame, and the defect is inside it. play.maar.world's canvas should follow its own element's width, not window.innerWidth; until it does, any page embedding that player at any size gets the same 14px overflow. cross-origin, so no stylesheet or script in this repo can reach it. flagged for the orbiter/player repo, not fixable here.
2026-07-31T16:02Z  MW-11  DONE     design/logo-grid-styled                    .logo-grid is a legacy class the migration carried over with NO rule in this build - three logos stacked at whatever size each file happened to be. invisible while two of the three were broken hotlinks; restoring the UArtes mark made it plain, a 110px mark under a 3840px one. now a centred wrapping row on a common 72px height, which is what makes logos of different proportions sit together. 72 so nothing upscales - the smallest file is 112px tall.
2026-07-31T16:11Z  MW-11  DONE     design/scattered-deck                      the owner: the cards are all perpendicular, they are in the deck but they don't need to be perfect - continue the dada idea. mark.css already settles it: a cut word is 'a piece of paper someone put down, and paper does not land square', and a sky sounds card IS paper. six angles, ceiling 1.8deg, chosen from the card's own path and FROZEN via cardTiltClass in lib/mark.mjs - never Math.random, which would reshuffle the deck on a back-navigation.
2026-07-31T16:11Z  MW-11  NOTE     design/deck-tilt-shift-measured            the scatter needs a flat draw or it is a lean, not a scatter. these seeds are FNV-1a's worst case - 34 paths differing only in a 2-digit number and a roman numeral near the end - and the first shift put 12 of 34 on one angle and tipped the whole grid left. counted every shift 0-26 over the real seeds: 10 is flattest (6/4/6/4/6/8, chi-sq 2.0 vs 9.1) and splits 16 left to 18 right, which is what shows on screen.
2026-07-31T16:11Z  MW-11  NOTE     design/deck-tilt-opt-in                    scoped to the grid that asks for it: indexTilt on the index page record, passed to the tilt prop on patterns/card. /lab and the home page emit zero tilt classes - a deck is scattered, a table of contents is not. rotation is transform-only so the grid's rows and columns do not move and cards lean within their own cells; measured overhang is 10.1px each side at phone width against a 20px gutter, so no horizontal scrollbar. hover and focus square the card and raise it.
2026-07-31T16:23Z  MW-9   DONE     embeds/orbiter-player-card-001             first plantasia orbiter embedded on a card page, replacing the play.maar.world snippet. one card only (001 sky sounds.1 card i) - the owner's staged rollout; the other 33 still carry the old player. embeds via the existing ui/embed-consent gate as a new 'orbiter' provider, href is the same ?trackId= address the physical NFC card forwards to. verified live: transport, 104 bpm sync, planet and controls all render, play responds.
2026-07-31T16:23Z  MW-9   NOTE     embeds/orbiter-framing-is-allowed          measured, since the owner expected trouble: orbiter.plantasia.space sends NO x-frame-options and NO content-security-policy frame-ancestors, and carries no CSP meta tag either, so nothing blocks framing it. CORS is not the mechanism for an iframe and was never going to bite here - the frame keeps its own origin, so the app's own fetches are unchanged by being embedded. it is a plain <iframe src>.
2026-07-31T16:23Z  MW-9   NOTE     embeds/orbiter-gated-not-inline            the orbiter is gated like youtube, and that is a POLICY choice the owner can reverse. verify:links FIRST_PARTY is /(^|\.)maar\.world$/, so orbiter.plantasia.space is a third party by origin even though the owner owns it - an ungated iframe fires a cross-origin request on load and turns the gate red. inline instead of click-to-load is one line (widen FIRST_PARTY), but it spends the no-cookie-banner posture. gated costs one press on a thing you press anyway.
2026-07-31T16:23Z  MW-9   BLOCKED  embeds/orbiter-cold-start                  TWO THINGS TO FIX IN THE ORBITER BEFORE THE OTHER 33 GO OVER. (1) cold start is ~10s and the frame is BLACK for all of it - no splash, no spinner - so an embedder's visitor presses play and stares at nothing. (2) two orbiter instances on one page froze the renderer outright; it is webgl plus an audio engine, so a page must never mount more than one. both belong in the orbiter app, not here, and both bite hardest for the public embed the owner wants.
2026-07-31T17:02Z  MW-9   DONE     embeds/orbiter-inline-all-33               owner's decision 2026-07-31: drop the click-to-load gate and embed the orbiter directly on every card page. 33 of 34 - the wild card has no track_v2_id and never had a player. the id is NOT copied onto the collect records; the route joins to the NFC record on suit_title + card_title, so one card keeps one id in one place. loading=lazy is load-bearing now: the orbiter is webgl + an audio engine and takes ~10s to boot.
2026-07-31T17:02Z  MW-9   NOTE     embeds/orbiter-first-party-widened         verify-links FIRST_PARTY widened from maar.world to also match plantasia.space, which is what lets the inline embed keep the gate green. same operator, not a third party in the sense the check polices. this is a POLICY change, not a bug fix - it is the single line that decides whether 33 pages fire a cross-origin request before the visitor asks, and it is commented in place so the next reader finds the reasoning at the constant.
2026-07-31T17:02Z  MW-9   BLOCKED  privacy/orbiter-fonts-and-storage          MEASURED, NOT ASSUMED, because the owner asked 'do we even have cookies there'. cookies: ZERO, they were right. but the orbiter pulls fonts.googleapis.com + fonts.gstatic.com on load - a real third party, the visitor's IP to google - and writes localStorage audioProfile + orbiters:network-speed-hint:v1, sessionStorage uniqueId, and IndexedDB firebase-heartbeat-database + firebaseLocalStorageDb. on 33 card pages the privacy page's 'no third party, nothing stored' is now untrue.
2026-07-31T17:02Z  MW-9   NOTE     privacy/gate-cannot-see-inside-a-frame     the green on 'no third-party request fires on page load' is now a weaker claim than it reads. verify:links scans the html and css THIS build emits; it cannot see what an embedded app requests once running, and never could. before today nothing cross-origin was framed on load so the gap was theoretical. the fix that closes it for real is in the orbiter: self-host Inter instead of google fonts, and drop the firebase heartbeat. then inline embedding is clean.
2026-07-31T17:23Z  MW-9   DONE     content/snippet-note-retired               owner removed the Snippet heading and the 'please unmute your device and press PLAY button / player optimized for chrome and firefox' note from all 34 collect card pages. every word described the play.maar.world excerpt player; 33 of these pages embed the maar orbiter now, which is not a snippet, has its own transport and is not chrome-and-firefox-only - the copy told the visitor to press a button that is not on the page.
2026-07-31T17:23Z  MW-9   NOTE     content/snippet-removal-is-durable         deleting it from the 34 records alone would NOT have held: CARD_TAIL in scripts/migrate-pages.mjs is what writes them, so the next migrate:pages would have put all 34 back silently. generator and output both changed. the dead .prose .card-player-note rule went with the markup - a rule matching nothing reads as live styling and hides that the element is gone.
2026-07-31T17:23Z  MW-7   NOTE     content/exclusions-handle-lists            author-content-expectations EXCLUSIONS gained two things this needed. (1) url may be a RegExp, so one entry covers all 34 card pages instead of 34 copies - 102 entries once heading, link and text are each counted, which would bury the six real one-offs. (2) a drop() for the assertions that are LISTS: headings and links had no exclusion path at all, so a deliberately removed heading could only be handled by editing the production manifest, i.e. by lying about production.
2026-07-31T18:48Z  MW-11  DONE     design/card-lean-is-the-default            owner asked for the inverse of the deck on every OTHER card: square at rest, leaning when you reach for it. now the default for every card on the site - home, documentation, any grid - and /collect/cards keeps the deck arrangement as the opt-in exception via indexScatter. one frozen angle per card serves both, two magnitude sets: --card-tilt for a resting scatter (subtle, keeps near-zero values) and --card-lean for a hover response (1.1-2.4deg, no near-zeros).
2026-07-31T18:48Z  MW-11  NOTE     design/card-lean-every-card-has-a-seed     leaning is no longer opt-in, so every card needs a frozen angle - and the home grid passes no seed, which would have made it the one grid on the site that did not respond. cardTiltClass now takes seed ?? href: href is always present and unique per card. the hash salt stays 'card-tilt' after the class rename to card--lean-N, so no card's angle was re-rolled by what was only a rename.
2026-07-31T18:49Z  MW-11  NOTE     design/no-transform-transition-on-cards    THE TRAP, measured in chrome 150. if transform is in the transition list and EITHER endpoint holds an unresolved var() - rotate(var(--card-lean)) - the new value never applies at all: computed transform stays the identity matrix and the card never leans. it reads as a cascade bug because the rule matches, z-index from the same rule applies, and the property resolves if you ask. @property and higher specificity both failed; removing transform from the transition is the only fix. see card.css.
2026-07-31T18:49Z  MW-11  NOTE     design/card-lean-respects-the-spec         no shadow and no scale on hover, deliberately: the card token block says a card has NO BORDER AND NO SHADOW and hover raises the field one step 'rather than scaling, tinting or shadowing anything'. the lean is the owner's addition to that rule; lifting or shadowing too would be quietly rewriting it. the spec's 1px press survives - :active restates the rotation, since transform is one property and a bare translateY would snap a leaning card straight.
2026-07-31T19:17Z  MW-7   DONE     content/58-warnings-cleared                verify:content 58 -> 0, suite 74/1/1 -> 75/0/1. of the 58, FORTY-SIX were the check disagreeing with itself and are fixed check-side; twelve were owner decisions nobody had written down and are now EXCLUSIONS with a reason and a ledger reference each. no assertion deleted, routes/manifest.production.json untouched, all four meta-checks still green, and the audit in author-content-expectations now uses the same floors verify:content does instead of stricter equality it could never satisfy.
2026-07-31T19:17Z  MW-7   DONE     content/head-counted-as-body               THE BIGGEST CHECK-SIDE FAULT, on all 130 pages. plainText strips tags but KEEPS the text inside them, so <title>Music - MAAR WORLD</title> counted as 19 chars of production BODY text - while the build side is measured inside <main>, which cannot contain a title. every minTextLength floor was inflated by the length of its own page title. <head> is a named CHROME region now. four of the eight text shortfalls were this to the character: collect/about prod 32 = title 27 + body 5, and the build is 5.
2026-07-31T19:17Z  MW-7   DONE     content/icon-ligatures-in-body             normaliseHeading has always dropped material-symbols spans from HEADINGS - the font is banned so 'nature_people Music' is not a string any reader saw - and left the identical span in body copy, where the ligature name counted as text. the same argument cannot be true for a heading and false for a paragraph. now a CHROME region, applied once to the whole body. /music 175 = title 19 + ligature 14 + 142, and the build is 142.
2026-07-31T19:17Z  MW-7   DONE     content/headings-compare-decoded           the 33 'soundscapes & music' warnings. production's jekyll HTML carries a bare & in the heading; astro escapes it to &amp;, as it must - a bare & to satisfy a checker is invalid HTML. plainText decodes neither, so the page renders the heading and the check called it missing. headings and contains now compare through comparable() in lib/html-text.mjs, on BOTH sides. lengths still use plainText, frozen into the manifest. 2 selftest cases: escaped passes, genuinely different still fails.
2026-07-31T19:17Z  MW-7   DONE     content/stale-checkout-links               /tree's missing soundcloud link was the BASELINE being wrong, not the build. ../tree.maar.world/_site is stale: 398 chars against the frozen manifest's 354, the only page where the two disagree on LENGTH. it still carries the old link-in-bio set. a link is asserted now only if manifest.production.json also records it - that list is whole-page, so a strict superset, and the intersection can only drop what production does not serve. across 130 pages it drops exactly that one.
2026-07-31T19:18Z  MW-7   DONE     content/embeds-is-a-floor                  the embed count was an EXACT match for no reason anyone wrote down, so the three videos the owner asked for in MW-9 content/videos-added were reported as content LOSS on the pages that had gained them: /landings 6 vs 4, lab/en and lab/es orbits-and-bodies 2 vs 1. now a floor, as images already is - an embed disappearing still fails. an extra third-party request on load is verify:links' job and it polices by ORIGIN, which a count cannot. paired selftest case so it cannot drift to unchecked.
2026-07-31T19:18Z  MW-7   DONE     content/twelve-exclusions-recorded         the owner decisions nobody had written down, each with a reason and a ledger reference in EXCLUSIONS. 8x disqus noscript link on lab en/es dadada+ip-1..3 - the embed is pasted into the ARTICLE BODY there, so the page__comments chrome strip never reached it. eng/esp-feedback embeds 1->0, retired google forms. /subscribe heading+text+link, mailchimp retired. /interplanetary-players text 122 - production prints an escaped redirect doc as visible text. /tree text 44. see the table.
2026-07-31T19:18Z  MW-11  BLOCKED  content/home-lost-ten-photographs          THE ONE EXCLUSION RECORDING REAL CONTENT LEAVING A PAGE rather than a check disagreeing with itself - needs the owner's yes. production's home carried ELEVEN photographs, which MW-11 built into ui/carousel over three sessions. family 01 then cut the section: homeMediaSections() in scripts/lib/home-family.mjs drops section-block--photos for patterns/collage-field. only 2024_ss-2.jpeg survives, promoted to the feature cover; the build serves 5. recorded as images:10, reversible in one function.
2026-07-31T19:18Z  MW-11  DONE     shell/nav-one-row-hamburger                area row is now HEADER_AREAS in src/config/site.ts = collect alone; maar was the brand link repeated, tree is link-in-bio reached directly. below 1024px areas+sections+language collapse into one <details> hamburger (still zero application js) — header is a single 76px row at 320/360/390/414/600/768/900/1023/1024/1280/1745, measured in-browser; it was two rows on a phone. switch is 1024 not 900: the wide row needs 944px and wrapped 900-943. SiteHeader.astro + shell.css
2026-07-31T19:30Z  MW-11  DONE     shell/collect-cards-and-icons              SECTIONS.collect gains Cards (/collect/cards, playing_cards icon) ahead of Docs, which gains the description icon the legacy collect header leaked beside that same label. Cards is an addition to the measured legacy inventory, not a restoration: legacy collect reached the deck only via a bandcamp Buy Now that MW-1/MW-6 forbid. Also splits aria-current, which painted 2 chips identically on /collect/cards: page keeps the pigment field, true gets ink + a pigment underline. site.ts + shell.css
2026-07-31T19:47Z  MW-11  DONE     design/page-family-02-lab                  Lab renders as page family 02 not an article. LabIndex.astro: header row with derived stats, a lead entry, year groups of card.article. patterns/card gains variant article plus wide, cover numeral, cover state badge, meta above title, tag chips. Covers are each article own image; 5 pieces have none and draw the plate. Frame 4:3 chosen by measuring the 5 real images, 0.81 to 1.79. GlyphRun set was 6 glyphs DM Mono does not ship, measured; now the 3 it does.
2026-07-31T19:54Z  MW-11  DONE     collect/landing-family                     new family 'collect' (schemas.mjs enum, families/Collect.astro, COLLECT_LANDING in site.ts). 3 hero--dark plates + a div.card the theme drew as nothing become 2 card.entry side by side at 900px+; the '25 physical cards available' plate is deleted, its bandcamp link survives on the offer card. hero--dark loses its 4% wash on dark (kept on paper) and hero__content stops stretching, which is why 'What is an Orbiter?' was 926px and is now 186px beside Collect's 176px. 2 hr dropped.
2026-07-31T19:57Z  MW-11  NOTE     i18n/scope-decided-by-owner                owner's answer 2026-07-31 to 'which pages need spanish': ALL FOUR groups - the 14 public-facing core pages, the 3 legal ones, the 34 collect card pages and the 9 documentation pages. 60 in total, on top of the 10 lab pairs that already exist. and 'who writes it': I DRAFT, THE OWNER EDITS. so every es page in src/content/authored is a draft awaiting the owner's pass, not finished copy - the risk they accepted is that a draft which reads fine is the hardest kind to notice is not theirs.
2026-07-31T19:57Z  MW-11  NOTE     i18n/es-url-convention                     spanish pages live at /es/<same path>: /es/about, /es/lab, /es/collect/cards. english keeps the bare path because that is where production served it - there is no /en/ prefix to add without moving 85 live URLs. the lab keeps /lab/en/ and /lab/es/ because production served it that way. slugs are NOT translated under /es/ - the three lab pairs whose slugs diverge do so because the SOURCE files were authored that way, not by design, and an identical slug is checkable.
2026-07-31T19:58Z  MW-11  DONE     i18n/translation-of-seam                   translationKey needs the key on BOTH halves, and outside the lab the english half is a MIGRATED record - migrate-pages wipes and rewrites src/content/migrated on every run, so a key added there lasts until the next migration. that is why the 10 lab pairs were the only ones: both halves are migrated and the migration computes the key for both. new field translationOf on the authored half names the page it translates. publishing a translation is ONE file in authored/, no migration run.
2026-07-31T19:58Z  MW-11  DONE     i18n/dangling-relation-fails-the-build     a translationOf naming no page does NOT error - it just makes alternatesFor return nothing, so the page renders exactly like one with no translation and a published spanish page becomes invisible from its english half. that is the silent-failure shape translationKey was STORED to avoid, so validateTranslations() runs in getStaticPaths and throws. a typo in an outputPath stops the build instead of quietly unpublishing a page. 6 selftest cases, both directions of the edge included.
2026-07-31T19:58Z  MW-11  DONE     i18n/verify-translations                   new check, 4 assertions, composed into npm run verify (75 -> 79 passed). the hazard: a page can declare lang es, carry a translationOf, render a switcher and a full hreflang set, and still BE THE ENGLISH TEXT - every signal a machine reads says spanish and the words say otherwise. same shape as arch/i18n-as-a-relation's defect, and the failure mode of draft-then-edit. NOT language detection, and it says so: it proves a translation is not a COPY, exactly, with no dependency.
2026-07-31T20:03Z  MW-11  DONE     i18n/first-ten-drafts                      10 of the 60 spanish pages drafted in src/content/authored/es: about, lab, music, calendar, landings, bookings, radio, privacy, collect/privacy, collect/docs/mw/terms. all DRAFTS awaiting the owner's pass. the three legal ones say so in a comment at the top of the file, in spanish. /es/lab is the page design/page-family-02-lab's exclusion already assumes exists - the ten spanish lab entries it moves off /lab land there. verify 79 passed 0 failed 1 skipped.
2026-07-31T20:03Z  MW-11  NOTE     i18n/indexes-are-per-language              the lab index filter the design session wrote - an index lists its own language and only its own - is applied at  in the page route now, so it holds for the documentation and card indexes too and for indexes that do not exist yet. without it /es/collect/documentation would list the ten english documents beside the spanish ones, which is the twenty-entry lab page that note describes. english indexes are unchanged: every indexed english record is lang en.
2026-07-31T20:03Z  MW-11  NOTE     i18n/indexes-are-per-language-corrected    the previous entry lost a word to a shell backtick and reads "applied at  in the page route". the place is the `listed` constant in src/pages/[...page].astro - one filter, all indexes. appending rather than editing because the ledger is append-only and a check enforces it.
2026-07-31T20:04Z  MW-11  DONE     design/lab-language-pin-and-copy           Lab index now lists its own language only: 10 EN entries on /lab, 10 ES on /es/lab, switched by the header chip that translationOf already relates. Owner instruction 31 Jul. Ten Spanish headings and their text recorded as a per-page exclusion, and drop() in author-content-expectations now takes a values set because it used find and would have applied only the first. Lead entry is LAB_PINNED by translationKey, one pin resolving in both tongues. Heading, lede, months and labels move to LAB_INTRO.
2026-07-31T20:19Z  MW-11  DONE     cards/black-field                          --card-field goes from --sf-raised to --sf-base on dark: a 7% ink wash reads as a grey tile laid over a black page rather than part of it. --card-field-hover drops --sf-raised-2 to --sf-raised so hover is still ONE step from rest, and is now the only thing that ever paints a field. Paper and print restore the original pair in their own blocks - a black card on paper is an inversion, not an integration. tokens.css only; 47 card classes still drawn.
2026-07-31T20:20Z  MW-11  DONE     i18n/sixty-drafts-complete                 all 60 spanish pages the owner asked for are drafted in src/content/authored/es, plus collect/about: 14 public-facing core, 3 legal, 9 documentation, 34 collect cards. 61 files, 72 of 85 english pages now have a spanish half. EVERY ONE IS A DRAFT AWAITING THE OWNER'S PASS - the legal three and the two with open questions say so in a comment at the top of the file, in spanish. verify:translations 4/4, verify:content 5/5, selftest 117/117.
2026-07-31T20:20Z  MW-11  NOTE     i18n/two-questions-in-the-drafts           two things marked in the files instead of decided quietly. (1) collect/docs/orbiters/how-to-use says 'currently support 7 modes' and then lists EIGHT, 0 to 7 - la lista se tradujo entera y el numero quedo intacto, because which of the two is wrong is a question about the product. (2) /privacy and /collect/privacy are translated FAITHFULLY, not corrected: MW-9 privacy/orbiter-fonts-and-storage says two of their claims stopped being true. fixing it in spanish only would make the halves disagree.
2026-07-31T20:20Z  MW-11  NOTE     i18n/build-guard-is-a-warning              validateTranslations THREW in getStaticPaths until it was seen to misfire. migrated/ is regenerated wholesale by migrate-pages, so the pages a translation points at are legitimately absent between runs - and when 34 collect card records were regenerated away under 34 authored spanish translations of them, a correct check turned an ordinary window into nobody-can-build. now a console warning at build time; verify:translations still FAILS on it and CI runs it, so nothing ships past.
2026-07-31T20:21Z  MW-11  BLOCKED  links/dropbox-on-load-returned             NOT THIS SESSION'S, recorded so it is not lost. verify:links went red at 21:15 when migrate:pages regenerated src/content/migrated: 4 on-load references to www.dropbox.com came back on 8 pages - revx000, rthw00, skyl0, skyl00, skyl000, sphe0 and lab/en+es/ip-orchestra. the migration used to strip them and this run did not. it is the one red check in the suite; src/content/authored has none. whoever owns the current migrate-pages edits should look, not a translator.
2026-07-31T20:28Z  MW-11  DONE     lab/covers-from-the-articles               Lab covers are each article own picture, owner artwork 31 Jul: EAC photo for helix, orbit-bodies for orbits and bodies, one plate for all three interplanetary ancestors, nornas for dadada. Zero cards left on the hatch plate. Frame re-measured to 3:2 now the set is 7 images, six of them landscape and the middle exactly 1.50. Orchestra poster is a transparent PNG so it is contained on black, new coverFit prop. Both EAC photos also render inside the helix article.
2026-07-31T20:28Z  MW-11  DONE     migration/self-hosting-seam-repairs-migrate npm run migrate:pages could not reproduce its own output: legacy card_image is a dropbox URL and imagePath was tightened 30 Jul to demand a first-party path, so the migration DROPPED all 34 collect card records, which the frozen route contract requires. The self-hosting rewrite now lives in migrate-pages where every other legacy-to-local decision lives, keyed on basename to media, plus one rename for uartes. Dead-legacy-img check now also accepts media. verify back to 79 pass 0 fail.
2026-07-31T21:01Z  MW-11  DONE     content/delete-migrate-pages               scripts/migrate-pages.mjs DELETED and npm run migrate:pages removed. The script had fallen behind the tree it generated, so a run was a revert, not a regeneration: twice on 2026-07-31 it destroyed all 34 collect/cards records (URLs printed on physical NFC cards) and reverted hand-made page structure to dead Jekyll markup. src/content/migrated/** is now hand-maintained - AUTHORING.md and content.config.ts reversed, both said never hand-edit. CI never ran it. verify 79/0.
2026-07-31T21:05Z  MW-11  DONE     i18n/mirror-rule-and-map                   the owner could not find a page's other half - 'everything is spread'. THE RULE IS NOW ONE LINE: authored/es/<path> is the translation of migrated/<path>. it already held for 60 of 61 files; es/tree.md moved to es/tree/index.md and verify:translations asserts it (5 assertions now, ~20 decoded as the space it encodes). npm run i18n:map prints every english file beside its spanish one, --todo for the gaps. suite 80 passed 0 failed.
2026-07-31T21:06Z  MW-11  NOTE     content/migrate-pages-retired-not-lost     correction to content/delete-migrate-pages: the script is not gone, it is OUT OF THE PROJECT at ../_retired/migrate-pages.mjs with a README stating why it must not be run. Outside maar-world/ deliberately - no npm script, CI job or import reaches it. docs/AUTHORING.md points there. Kept because it is the only record of how 95 records were derived from the legacy checkouts; retired because a run reverts rather than regenerates. verify 80/0.
2026-08-01T08:33Z  MW-15  DONE     infra/local-maar-world-worktrees           dev moved off localhost to https://local.maar.world:4321 and into worktree slots, main as trunk (commits on main now blocked by the shared hook). a second worktree-monitor instance watches this root on :4179 - its root and host are one-per-process, so :4178 cannot cover it. toolkit symlinked, not forked. docs/LOCAL-DEVELOPMENT.md is the setup; docs/adr/0002 is the why. verify 80/0.
2026-08-01T09:44Z  MW-15  DONE     docs/agents-skills-system                  adopted the standard .agents/ layout; docs/agent/ is gone. authoring, visual-language, design-authority became skills/; OPERATING-RULES split into /AGENTS.md (invariants) + .agents/AGENTS.md (workflow). docs/adr moved to .agents/decisions with NUMBERS PRESERVED - 0001/0002 are cited from code. deleted CONTENT-PIPELINE-NOTES and design-qa (dead state; their one open question is already a BLOCKED line). HANDOFF and the ledger stay - live state. see decisions/0003. verify 80/0.
2026-08-01T09:52Z  MW-15  DONE     scripts/retire-migration-generators        migrate-cards.mjs and collect-media.mjs moved OUT to ../_retired. one-time generators over the legacy checkouts; their output (36 card records, 192 media files) is tracked and hand-edited since, so a run reverts rather than regenerates - what made migrate-pages destroy card records twice. scripts/ stays: 22 of 24 files are the verify suite, ledger and assemble-public. resumability.md corrected - it cited migrate-cards as proof migration scripts are idempotent. verify 80/0.
2026-08-01T10:46Z  MW-13  DONE     layout/full-bleed-vs-scrollbar             the permanent horizontal scrollbar was full bleed measured with 100vw, which counts the classic scrollbar: .collage-header and .tree-hub overhung the layout by ~7.5px a side and documentElement.scrollWidth beat clientWidth by 8. .shell__main is now an inline-size container and both use 100cqi, the same width with the scrollbar already subtracted. measured 0 overflow on 10 pages x 5 widths, 390-1920. see shell.css.
2026-08-01T10:46Z  MW-13  NOTE     layout/tree-hub-stacked-forward            consequence of making .shell__main a container: it forms a stacking context, and .tree-hub reached BEHIND its parent with z-index -2 (photo) and -1 (scrim). a negative z-index cannot escape a stacking context, so the Tree page rendered as a black rectangle with the image still loaded and fixed. the same three layers are now 0/1/2 with .tree-hub__column positioned - identical render, and no longer dependent on what an ancestor does. see tree-hub.css.
2026-08-01T10:46Z  MW-14  DONE     design/marquee-rail-is-two-lanes           the marquee never looped: lane min-inline-size 100% is a percentage resolved against the rail, which is max-content sized FROM the lanes. Chrome cut the cycle by sizing the rail to ONE lane while it still held two - 7791px of box around 15583px of content - so the keyframe's -50% travelled half a lane and each cycle ended in a 3896px snap. now 100cqi against the viewport: rail is exactly 2 lanes, and the frame at wrap is identical to frame 0 on both marquees.
2026-08-01T10:46Z  MW-16  DONE     design/docs-index-glyphs                   docs are one surface now: 18 records went surface paper to dark, so nothing renders shell-paper any more. the Docs index drops its ten covers - dark photos of unrelated subjects that read as ten identical black rectangles - for one inline Material Symbol per article (DOC_GLYPHS in site.ts, ui/DocGlyph), the article's own opening sentence via lib/doc-excerpt, and read more / leer mas as the meta line. tutorials and terms have no prose paragraph and correctly show no excerpt.
2026-08-01T10:46Z  MW-16  NOTE     design/shell-paper-now-unused              flagging rather than deciding: collect/docs were the ONLY consumers of surface paper, so layouts/shell-paper and the [data-surface=paper] token block are now dead code, and verify:a11y measures contrast on the dark surface only - it reports the surfaces dist actually renders, so paper stopped being asserted the moment nothing used it. deleting the second surface is a design decision beyond MW-16's scope; BaseLayout still documents paper as the surface for document routes. owner's call.
2026-08-01T11:01Z  MW-16  DONE     design/shell-paper-removed                 owner's call once the docs were dark: the second surface is gone. deleted layouts/shell-paper, the [data-surface='paper'] token block, the legacy hero--dark paper exception, the surface field from the schema and from all 133 records, and the paper entry in verify-a11y SURFACES. @media print KEEPS the cream values and is now the only place they live - printing is a different question from theming. BaseLayout hardcodes data-surface=dark. verify 80/0.
2026-08-01T11:01Z  MW-16  DONE     design/docs-covers-deleted                 owner: 'you can remove them from the repository, they are old and we don't want them anymore'. the nine cover files are deleted from media/collect/img/docs/covers/ and the cover field with them - 20 records, ten articles x two languages. qr-technology.jpg STAYS in that folder: it is a diagram inside the ENT Cards article, not a card cover. the EXCLUSIONS entry and its copy in content-expectations.json now say deleted rather than retained, so the record matches the tree.
2026-08-01T11:13Z  MW-15  DONE     docs/readme-card-wording                   owner rewording: the README no longer states how many physical NFC cards exist, which codes they came from, or the exact URL count - the repo is public and those are details about objects in people's hands. the constraint itself is unchanged and stated more strongly (cannot be renamed, re-cased, redirected or retired), now pointing at routes/manifest.production.json as the frozen list. adds a Content section noting migrated/ and authored/ are both hand-maintained. verify 80/0.
2026-08-01T11:15Z  MW-11  DONE     i18n/spanish-filing-closed                 all 72 spanish records asserted: 61 at the mirror, 11 named in LEGACY_ES in scripts/verify-translations.mjs - the 10 lab pages filed area/es/slug, plus /esp-feedback which has no pair and must not be given one. that list is CLOSED, with a size assertion, else these checks are satisfiable by typing a path into it. /es/ prefix rule added, binding on NEW records only. verify 80 to 84, translations 5 to 9, selftest 117 to 123. rule in .agents/skills/maar-content-authoring/SKILL.md
2026-08-01T11:32Z  MW-11  DONE     i18n/origin-field-not-directory            route authorisation no longer reads a directory. new required field origin: migrated or authored on all 157 page records, and authoredRoutes() in scripts/verify-routes.mjs reads it instead of walking src/content/authored. that folder was the authorisation boundary, so ANY reorganisation moved a security decision by accident - this is the commit that has to land before the files move. both directions asserted in selftest. verify 84 passed 0 failed 1 skipped, no file moved, no url touched
2026-08-01T11:43Z  MW-11  DONE     i18n/content-tree-by-language              src/content is organised by LANGUAGE now: pages/en/** and pages/es/**, 85 and 72. migrated/ and authored/ are gone - they sorted pages by how they arrived, which is history nobody can infer, and spanish was split across both by three conventions. one rule instead: a record sits at pages/lang/outputPath, asserted over all 157 with no exceptions. NO URL MOVED - all 426 built files byte-identical before and after, routes/ untouched. verify 84 passed 0 failed 1 skipped. see .agents/decisions/0004
2026-08-01T12:06Z  MW-11  DONE     i18n/navigation-follows-language           the header sent a spanish reader back to english on their first click - SECTIONS is english-only and every page rendered that list, so /es/collect was reachable from no link on the site. navPathsFor() in src/lib/translations.mjs resolves each destination through the STORED relation, not an /es/ prefix: three lab pairs have translated slugs a prefix rule sends to a 404. untranslated destinations keep their english url. new assertion, falsified end to end - 626 leaks with it off. verify 84 to 85
2026-08-01T12:44Z  MW-11  DONE     i18n/canonical-url-one-spelling            the language switcher pointed at /es/index.html which the dev server 404s - owner reported. cause: THREE spellings of outputPath-to-URL. SiteHeader appended .html and special-cased only the site root, BaseLayout did the same for hreflang, and canonical came from the BUILD PATH not the record. one exported urlOf() in src/lib/translations.mjs now, used by all three. new assertion. no route changed, 426 files identical, verify 85 to 86
2026-08-01T13:28Z  MW-19  DONE     arch/collect-structure-once                collect/index renders from families/Collect.astro; both records carry copy only. 90 duplicated structural elements to 0, 2 elements of drift to 0. /es/collect gains its collage header, Spanish cards and Spanish form. verify:translations now asserts a Spanish body carries no structural markup, ratcheted over the 34 records still to convert.
2026-08-01T13:45Z  MW-19  NOTE     arch/ip-orchestra-partial                  ip-orchestra: dead theme markup out, prose links to markdown, ES adopts EN where they diverged. 62/61 structural elements to 49/49, drift 1 to 0. Removed 3 inline onsubmit handlers linking to a page that does not exist. Carousels, play embeds, contact form, logo grid and the h1 mark still in both bodies - they need a component, see HANDOFF.
2026-08-01T14:05Z  MW-19  DONE     arch/ip-orchestra-structure-once           ip-orchestra renders from components in both languages: 62/61 structural elements to 0/0. Added @astrojs/mdx v4 (owner approved) - an article interleaves prose and blocks so a fixed-slot family cannot express it. New: Carousel step/orbMedia variants, PlayEmbed, ContactForm, LogoGrid, SubscribeStub, EmbedGroup, Mark, config/articles.ts. STRUCTURED_ES 34 to 33.
2026-08-01T14:28Z  MW-19  BLOCKED  arch/radio-deprecated                      Owner: /radio is deprecated, delete both halves. Cannot: /radio is origin=migrated and its URL is in the frozen production manifest, so deleting it fails verify:routes (2 preserved routes), verify:content (production baseline) and verify:links (3 external links). Making them pass needs contract:relock --accept-removals, which AGENTS.md forbids as a way to turn a check green. Needs an explicit decision: relock, or keep the URL as a redirectTo stub like /interplanetary-players.
2026-08-01T14:51Z  MW-19  DONE     arch/landings-structure-once               landings and es/landings are one structure. 47 duplicated elements to 0 on both halves: six video plates became media/EmbedPlate, sixteen raw anchors became markdown links, the heading span became patterns/Mark. Facade wording moved to src/config/embeds.ts keyed by provider and language, the shared table site.ts predicted, and COLLECT_LANDING.video is gone. Video addresses named once in config/articles.ts. STRUCTURED_ES 33 to 32, closed count with it. verify 90 passed 0 failed 1 skipped
2026-08-01T15:00Z  MW-19  DONE     arch/bookings-structure-once               bookings and es/bookings are one structure. 9 duplicated elements to 0 on both halves: EPK viewer to media/PdfEmbed, form to the existing ui/ContactForm, empty spacer dropped. EPK address and endpoint named once in config/articles.ts. verify:build gained a third DRIVEN_MARKUP rule - the PDF reader was the one driven thing with no assertion. An anchor inside object made Astro emit 2 nameless links; the fallback is injected HTML now. STRUCTURED_ES 32 to 31. verify 90 0 1
2026-08-01T15:07Z  MW-19  DONE     arch/orbiters-structure-once               orbiters and es/orbiters are one structure. 49 duplicated elements to 0 on both halves: carousel to media/Carousel which gained autoplayMs, two players to media/PlayEmbed which gained a heading level, four bands to patterns/Band, images to media/Picture. Spacers grids cells and the swiper shell deleted - none matched a rule. Named Picture not Figure: STRUCTURAL_TAGS is case-insensitive so a component called Figure counts as a figure tag. STRUCTURED_ES 31 to 30. verify 90 0 1
2026-08-01T15:16Z  MW-19  DONE     arch/mark-tail-structure-once              the one-element tail: 11 pairs whose only structure was the marked word in the h1, now patterns/Mark on both halves, 21 records to mdx. Found a real drift - shared-culture highlights Access, cultura-compartida cut a different word. English wins so the Spanish highlights Acceso now. esp-feedback lost its raw paragraph and anchor. STRUCTURED_ES 30 to 19
2026-08-01T15:23Z  MW-19  DONE     arch/one-website-two-languages             new assertion: a page and its translation render the same element skeleton. The owner's rule made checkable - one website in two languages, not two websites. 62 of 71 pairs already identical; the 9 that are not are listed in DIVERGENT_PAIRS, closed at 9, shrinks only. Stronger than the no-markup rule: how-to-use has no markup in either half and still writes a list item where English writes a paragraph. Body only - header switcher and footer are per language by design. verify 90 to 92 passed
2026-08-01T16:00Z  MW-19  DONE     arch/pictures-have-no-language             the owner's bug, found and fixed. Five Spanish pages rendered NO section header - es/landings es/bookings es/orbiters es/about es/calendar - and the Spanish home drew one of three cards with no picture. Both were lookups keyed by an English-only path returning null on a miss: SECTION_COLLAGE and ARTICLE_COVER_FALLBACKS. Both now fall back to the language-stripped path. New check: a page and its translation show the same pictures, 69 of 71, whole document not just body. verify 92 to 93
2026-08-01T16:12Z  MW-19  DONE     arch/shell-speaks-readers-language         the shell was English on every Spanish page - nav, area row, the home buttons - and BaseLayout set chromeLang=en to say so honestly instead of fixing it. Four per-language shapes existed already and the navigation had none. SECTIONS and HOME_ACTIONS now carry labelEs read through one labelFor helper, reusing the TREE_LINKS pattern. chromeLang is the page language now. New check: a Spanish page names its navigation in Spanish. verify 93 to 94
2026-08-01T16:26Z  MW-19  DONE     arch/about-structure-once                  about and es/about are one structure, 7 duplicated elements to 0. The portrait and bio layout is patterns/EditorialProfile; the Spanish record's own note said to change the markup in both places by hand, which was the defect. Owner decisions: the Founder and Fundadora heading is removed in both languages, and its aria-labelledby went with it; the About nav entry is the info icon alone, keeping info as the accessible name. STRUCTURED_ES 19 to 18. verify 94 0 1
2026-08-01T16:30Z  MW-19  DONE     arch/music-calendar-structure-once         music and calendar converted, both halves each. New media/PlayFrame draws the bare player and its full-screen link from one query string - it serves ip-1 ip-2 ip-3 too, 14 frames in all. Thirteen Spanish frames said Play full screen in English; es/music was the only one translated properly, so its wording is now the component's and the other thirteen inherit it. calendar's facade comes from EMBED_FACADE with the google-calendar entry added. STRUCTURED_ES 18 to 16, DIVERGENT_PAIRS 9 to 8
2026-08-01T16:32Z  MW-19  DONE     arch/ip-ancestors-structure-once           ip-1 ip-2 ip-3 converted, both halves each - 46 duplicated elements to 0. Thirteen bare players became media/PlayFrame, prose anchors became markdown links, heading spans became patterns/Mark. The thirteen instrument query strings are IP_ANCESTORS in config/articles.ts, named once instead of once per language. The Spanish halves stop saying Play full screen in English. ip-2 stays in DIVERGENT_PAIRS - its two bodies still differ in prose, not markup. STRUCTURED_ES 16 to 13
2026-08-01T16:33Z  MW-19  DONE     arch/collect-docs-structure-once           tutorials ent-cards/nfc and orbiters/development converted, both halves each. Two youtube facades became media/EmbedFacade reading EMBED_FACADE by language; the prose anchors became markdown links; the heading spans became patterns/Mark. STRUCTURED_ES 13 to 10, 62 of 72 Spanish records now hold words only. verify 94 0 1
2026-08-01T17:06Z  MW-19  DONE     arch/radio-deprecated-removed              radio is deleted and 404s, on the owner's explicit instruction and their own hand on the relock. Both records gone; policy.json marks /radio and /radio.html drop with the reason; the three external links eepurl gumroad and the stream are recorded as reviewed removals; the contract is relocked at policy sha 5ad5bb98, preserve 355 to 353 drop 256 to 258; the content baseline is 130 to 129 pages. Closes the BLOCKED entry of 14:28. STRUCTURED_ES 10 to 9, DIVERGENT_PAIRS 8 to 7. verify 94 0 1
2026-08-01T17:19Z  MW-10  NOTE     pages-deploy-pipeline                      Added .github/workflows/deploy.yml: Pages via Actions, fires on verify success on main and rebuilds the verified SHA. No CNAME in the artifact — with Actions as source the domain lives in Pages settings. Repo Pages was build_type=legacy, status=errored: source switched to GitHub Actions. Needs a domain root (no base, root-absolute paths), so maar34.github.io/maar-world will not serve correctly. README Deployment section + host-canary recipe.
2026-08-01T17:28Z  MW-10  NOTE     host-cutover                               maar.world now served by maar34/maar-world. Pages source switched legacy->workflow; domain released from maar.world-site and claimed here; no DNS record changed (apex A + www CNAME already GitHub). Canary passed on the live domain: 70/70 card URLs direct 200 no redirect, %20-trailing-space URL 200, 4 PDFs application/pdf. verify/host-canary.json closes the last SKIP — verify is 95/0/0. Open: collect./tree. still serve legacy repos, no 301.
2026-08-01T17:51Z  MW-10  NOTE     redirect-shim                              scripts/author-redirect-shim.mjs builds redirect-only sites for collect./tree. from redirects.map: 52+2 stubs, 404.html as wildcard (Pages serves it for any unstubbed path, incl %20). 200+instant meta refresh, not 301 — Pages cannot emit one. npm run shim builds+verifies; shim:verify resolves all 127 map lines the way Pages would, PASS, 53 .html twins land on the canonical extensionless URL. Output dist-shim/, gitignored, pushed nowhere.
2026-08-01T18:05Z  MW-10  DONE     subdomain-redirects                        collect./tree. now redirect to maar.world. Shim deployed to new repos maar34/collect.maar.world-redirect (56 files) and tree.maar.world-redirect (6). Domains released from the originals and claimed by the redirect repos — certs carried over, no DNS change. Originals archived read-only, full history, one 'Delete CNAME' commit each (86d998e5, 202dc435). Live checks pass. Rollback in docs/ROLLBACK.md.
2026-08-01T18:13Z  MW-19  DONE     fix/docs-excerpt-mdx-import                the Documentation index rendered cards reading import Mark from components/patterns/Mark.astro, in both languages, on the live page. docExcerpt returns the first prose line of a body; an mdx body opens with its imports and the line cleared the character floor. Now skips import and export lines and strips JSX comments too - a converted Spanish record carries its translator note as a JSX comment, which would have printed next. Two selftest cases. verify 95 0 0
2026-08-01T18:25Z  MW-19  DONE     pages/collect-wrappers                     collect/cards and collect/documentation rewritten as mdx: the layout--articles div, section and header wrapper held nothing, the grid is drawn by the layout from indexOf. Heading span became patterns/Mark. STRUCTURED_ES 9 to 7, CLOSED_AT with it. Note: an ES translator comment in HTML form ships to the page AND its tag names are counted by elementSkeleton; the mdx form does not. See src/content/pages/es/collect/cards.mdx
2026-08-01T18:29Z  MW-19  DONE     pages/collect-docs-tables                  collect/docs/mw, .../releases/skysounds and .../ent-cards rewritten as markdown. Three raw tables became markdown tables; skysounds swapped nine hand-written row headers for one real column header row. ent-cards five nested divs around a picture, a heading and one sentence became those three things. All three headings now use patterns/Mark. STRUCTURED_ES 7 to 4, DIVERGENT_PAIRS 7 to 5, both CLOSED_AT with them.
2026-08-01T18:32Z  MW-19  DONE     pages/orbits-and-helix                     orbits-and-bodies converted to media/PdfEmbed plus two media/EmbedFacade; the media-frame wrapper around each facade was deleted, no stylesheet on the site ever drew it. helix converted to a new media/DiagramFrame, first-party so no facade. Addresses moved to ORBITS_AND_BODIES and HELIX in config/articles.ts. Fixed a real defect the pair check exposed: the English name was bold-opened and never closed, so it shipped with four literal asterisks. STRUCTURED_ES 4 to 2, DIVERGENT_PAIRS 5 to 4.
2026-08-01T18:35Z  MW-19  DONE     pages/dadada                               dadada converted to media/EmbedFacade plus media/PlayFrame. soundcloud added to EMBED_FACADE with its own Spanish label, which is what fixes lab/es/dadada saying listen on soundcloud in English; the full-screen link is Spanish now too. Attribution box with its copied inline styles is two markdown links. MDX rejects a bare br, error points at the paragraph end. external stays absent, only caller was /radio. STRUCTURED_ES 2 to 1, DIVERGENT_PAIRS 4 to 3.
2026-08-01T18:37Z  MW-19  DONE     pages/ip-orchestra-design                  ip-orchestra-design converted: poster to media/Picture, video to media/EmbedFacade, the two instruments to media/EmbedGroup plus media/PlayEmbed, the form to ui/ContactForm. Nineteen elements to zero. Poster has two fields in IP_ORCHESTRA_DESIGN because it is designed type, the TRANSLATED_ARTWORK case. Instrument headings are h3 now, not the h2 production served: they sit under a h3 and PlayEmbed defaults there, same as the sibling article. STRUCTURED_ES 1 to 0, DIVERGENT_PAIRS 3 to 2.
2026-08-01T18:41Z  MW-19  DONE     shell/embed-gate-english                   Found by opening a Spanish page, not by a check: ui/EmbedConsentScript built three English sentences in the browser on every language. Poster read plays from youtube, accessible name ended loads the player from youtube-nocookie.com, calendar said open the booking calendar here. All on top of a correctly translated facade. Words now come from GATE in src/config/embeds.ts, keyed by the lang the shell sets. Affects every gated embed, not only converted pages.
2026-08-01T18:43Z  MW-19  DONE     handoff/mw-19-complete                     MW-19 complete. All sixteen pairs converted, nine this session. STRUCTURED_ES is empty and closed at 0. DIVERGENT_PAIRS holds 2, both of them prose drift rather than markup: how-to-use writes a list item where its half writes a paragraph, ip-2 an extra list around the third instrument. Neither was ever on STRUCTURED_ES. HANDOFF.md rewritten with the four shipped defects found by opening pages, and the MDX traps.
2026-08-01T18:53Z  MW-19  DONE     copy/orbitador-not-orbiter                 Owner: Orbiters is the product name and stays; the common noun goes in Spanish. 39 files - both orbiters docs, ent-cards, tutorials, the Collect button and all 34 card records now say un orbitador, los orbitadores. Name, URLs, domain and header label untouched. New check matches an article before the word, since a name takes none; Orbiters Orchestra excluded by lookahead, comments cut first. Suite 95 to 96.
2026-08-01T19:01Z  MW-19  DONE     copy/orbitadores-is-the-name               Corrected: the product is Orbitadores in Spanish and Orbiters in English, two names for one product, a year old. The earlier name-versus-noun split was wrong and left the header saying orbiters on all 71 Spanish pages. labelEs, page title, h1 and remaining copy now say Orbitadores; Orbiters dropped from SHARED_CHROME_WORDS. Check now reads body, title AND the built header. Only Orbiters Orchestra, the plantasia address and the frozen URLs keep English.
